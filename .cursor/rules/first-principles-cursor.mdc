---
description: 第一性原理 — Rules 与 Skills 的分工、调用方式、歧义与验证闭环（本仓库 AI 协作总纲）
alwaysApply: true
---

# 第一性原理：在本仓库如何用 Cursor 提效

## 本体论：两层上下文

| 层级 | 是什么 | 放什么 | 何时生效 |
|------|--------|--------|----------|
| **Rules**（`.cursor/rules/*.mdc`） | 短、硬、可执行的不变量与边界 | 编码风格、安全基线、禁止事项、行为约束（如 Karpathy 四条） | 按 `alwaysApply` / `globs` 自动注入 |
| **Skills**（`.cursor/skills/<name>/SKILL.md`） | 可复用的多步骤工作流与领域套路 | UI 流程、规划写法、TDD 循环、头脑风暴结构等 | 由描述匹配或你显式 `@技能名` / 命令面板选用 |

**原则**：Rule 不写教程长文；Skill 不替代项目安全/风格红线。二者互补，避免把 Rules 撑成「第二份文档」、也避免把 Skills 写成几句空泛口号。

## 调用策略

1. 默认会话已加载 `alwaysApply: true` 的规则，无需重复粘贴。
2. 任务明显属于某一领域（如「上新页面 UI」「先写测试」「要写 PLAN」）时：**主动声明**将使用哪个项目 Skill（例如在对话里说「按 `ui-ux-pro-max`」或 `@ui-ux-pro-max`），减少模型隐式跑偏。
3. 单一 Skill 装不下时：拆任务，**一次只激活一个主 Skill**，避免上下文打架。

## 歧义与成本

- 需求存在多种合理理解时：**先问最短澄清问题**，再改代码；不要用更大 diff 去「猜」意图。
- **token 即成本**：优先读相关文件片段，拒绝无目的全库扫描；输出只写完成任务所必需的内容。

## 可验证的成功标准（声明式目标）

把「实现功能」改写成可检查目标，例如：

- 「加校验」→ 先写失败用例/断言，再实现到通过。
- 「修 bug」→ 先最小复现（测试或脚本），再修到通过。
- 「改 UI」→ 列出可观察验收（布局、交互状态、a11y 要点）。

多步骤任务用简短计划 + 每步的 verify，便于循环执行而不是一次性猜完。

## 与本仓库内置 Skills 的映射（按需）

| 场景 | 建议 Skill |
|------|------------|
| 界面/组件视觉与交互质量 | `ui-ux-pro-max` |
| 复杂改动前先落文件/里程碑 | `planning-with-files` |
| 网页语义、性能、a11y 基线 | `web-design-guidelines` |
| 用户可见文案、错误信息「人话」程度 | `humanizer` |
| 方案未定型、需发散与权衡 | `brainstorming` |
| 以测试驱动开发与回归 | `test-driven-development` |

修改 `.cursor/skills/` 后应同步更新本表或对应 SKILL 的 `description`。
