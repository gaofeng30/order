---
description: 本仓库统一协作入口：AGENTS.md、OpenSpec 与项目 skills
alwaysApply: true
---

# 项目协作入口

开始任何任务前，先读取并遵守仓库根目录 `AGENTS.md`。

- 单个 change 的规格与验收事实源：`openspec/changes/<change-name>/`
- 项目执行流程事实源：`.agents/skills/`
- OpenSpec Cursor 命令：`.cursor/commands/opsx-*.md`

规划、实现、验证、集成分别使用 `order-plan-change`、`order-implement-tdd`、`order-verify-change`、`order-integrate-change`。Cursor 同名 skills 只是薄入口；如与 `AGENTS.md` 冲突，以 `AGENTS.md` 为准。
