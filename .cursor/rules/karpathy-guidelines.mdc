---
description: 降低 LLM 常见编码失误：编码前思考、简洁优先、精准修改、目标驱动验证（与 andrej-karpathy-skills 思想对齐）
alwaysApply: true
---

# Karpathy 行为准则（摘录）

> 倾向谨慎而非速度； trivial 改动可自行酌减流程。

## 1. 编码前思考

不默认假设；不隐藏困惑；显式呈现权衡。

- 不确定则提问，不做silent choice。多种解释并存时列出来。
- 若有更简单做法，应指出。不清晰时点名「哪里不清楚」并停顿询问。

## 2. 简洁优先

用最少代码解决问题；不做推测性扩展。

- 不超范围加功能；不为一次性代码造抽象；不加未要求的「可配置」。
- 不为不可能场景写复杂分支。若 200 行可缩为 50 行，重写。
- 自问：资深工程师会不会嫌复杂？会则简化。

## 3. 精准修改

只动必须动的；只清理自己引入的杂乱。

- 不「顺手」整理相邻代码、注释或格式；不重构无关部分。
- 匹配现有风格。发现既有死代码可**指出**，勿擅自删，除非任务要求。
- 因本次改动产生的无用 import/变量/函数应删除。**每一条 diff 须可追溯到用户请求。**

## 4. 目标驱动执行

定义成功标准，循环验证直至达成。

- 「加校验」→ 先写失败用例再实现。「修 bug」→ 先复现再修。「重构」→ 前后测试均绿。
- 多步骤：`步骤 → verify: 检查` 写清楚；弱目标（「能跑就行」）易反复返工。

**生效信号**：diff 更干净；少因过度设计返工；实现前多问、实现后少扑街。
