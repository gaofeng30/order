## 1. Boundary and approval

- [x] 1.1 Confirm proposal/design/spec/tasks/checkpoint consistently declare the single status-read outcome, `gate_type=W3`, `ui_level_target=UI0`, DRAFT `ui_level_actual=NOT_RUN`, exact base, owner/worktree, owned and read-only paths, integrated dependencies, non-goals, required external assets none, local MySQL Gate, real WeChat `NOT_REQUIRED`, and the single ACCEPT/REJECT verdict; run `openspec validate serve-miniprogram-primary-phone-status --strict` and obtain explicit main-session approval before recording `DRAFT → APPROVED`.
  - Evidence: the main session completed full DRAFT review and explicitly returned `APPROVED`. `openspec validate serve-miniprogram-primary-phone-status --strict` exited `0` with `Change 'serve-miniprogram-primary-phone-status' is valid`; the same Writer records `DRAFT → APPROVED → IMPLEMENTING`. This proves implementation admission only; behavior and W3 Gates remain `NOT_RUN`.
- [x] 1.2 Before implementation, confirm branch/base/worktree and sole-writer ownership still match, migration/provider/router/main remain read-only, and no POST bind, P1-P5, employee/merchant/profile/checkout/order/payment/frontend scope has entered the change.
  - Evidence: branch `codex/serve-miniprogram-primary-phone-status`, worktree `/Users/vivix/.codex/worktrees/serve-miniprogram-primary-phone-status/order`, HEAD and merge-base all match exact `a5728022c8e497947267f1b8db5ff50983c03be9`; read-only diff guard over migrations, provider, phone repository SQL, router and main exited `0`. Status contains only the change DRAFT before the first Red; no excluded scope entered.

## 2. Red

- [x] 2.1 Add focused service/handler tests for Status mask reuse, bound/unbound, exact JSON string/null shapes, empty-body/no-Content-Type acceptance, non-empty-body precedence, exact single Authorization, unknown/expired/auth-store failure, phone-store failure, no-store on 200/400/401/503, zero provider/bind/write calls and sanitized evidence; before production changes run `GOPROXY=off GOTOOLCHAIN=go1.26.5 go test ./services/api/internal/identity -run '^TestPhone(Status|HandlerStatus)' -count=1` and record the first target-behavior failure.
  - Red evidence: after adding and formatting only owned service/handler tests, the exact focused command exited `1`; first decisive compile failure was `phone_handler_test.go:168:10: undefined: PhoneStatus`, followed by `PhoneService.Status undefined`. Production bytes were unchanged; this proves the status operation/representation is absent, not Green or W3 behavior.
  - Green/Refactor evidence: adding only `PhoneStatus`/`PhoneService.Status` and the existing handler's GET path made the unchanged command exit `0` (`identity 0.485s`); without contract changes, the identical command passed again (`identity 0.231s`). Tests prove bound/unbound mask/null shapes, empty-body/no-Content-Type, non-empty-body precedence, exact Bearer, no-store on 200/400/401/503, sanitized 401/503, and zero provider/bind calls. Real MySQL and router/smoke remain separate pending Gates.
  - Sequence correction: GET was initially registered while obtaining the handler Green, before task 2.2's route-level Red. The registration line was explicitly removed, status handler tests were isolated through a package-local test route, and the unchanged 2.1 command still passed (`identity 0.438s`). No route Red is claimed from the earlier run; production registration remains absent until 2.2 records a real 404.
- [x] 2.2 Add router/smoke assertions for only `GET /api/v1/me/primary-phone`, absence of broad/unversioned/profile routes, unchanged POST bind and anonymous session/catalog/menu behavior; before route registration run `GOPROXY=off GOTOOLCHAIN=go1.26.5 go test ./services/api/internal/httpapi -run '^TestRouterPrimaryPhoneStatus' -count=1` and `GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/smoke.sh`, recording the first real route failure.
  - Red evidence: after the sequence correction removed production GET registration, the exact router command exited `1` with `router_test.go:538: status = 404, want 200`. The exact smoke command also exited `1` with `primary-phone status route returned 404, want 401`. Both failures are the absent production route; service/handler focused behavior remained Green through its package-local test route.
  - Green/Refactor evidence: restoring only `engine.GET("/api/v1/me/primary-phone", handler.status)` made the unchanged router command pass (`httpapi 0.438s`) and smoke return `smoke: PASS`; unchanged reruns passed again (`httpapi 0.194s`, `smoke: PASS`). The focused identity command also passes with final registration (`identity 0.435s`) after removing the temporary package-local duplicate-route fixture. No router/main byte changed.
- [x] 2.3 Add a non-skipping isolated MySQL 8 integration test using actual Authenticate, PhoneService and HTTP handler for exact bound/unbound, unknown/exact-expiry sessions, two-user isolation, auth DB error, phone DB error, zero provider/bind calls and before/after equality of all affected user/session values; update only the dedicated runner selection if required, run `GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/miniprogram-phone-integration.sh` before production status code, and record the first W3 failure. Mock/SQLite is invalid W3 evidence.
  - Red evidence: because the service implementation was introduced during 2.1, it was transparently reset to return `ErrUnavailable` before this W3 Red; no Red is claimed against already-Green bytes. With the new test using v10 migration, actual MySQL session creation/authentication, bound-phone seed and HTTP handler, the exact dedicated runner exited `1` on isolated MySQL 8 (`order-mysql-w3`, ownership marker `YES`): `mysql_integration_test.go:161: real MySQL primary-phone status code = 503, want 200`. Setup reached production `Status`, so this is the missing production read behavior rather than fixture setup or a skipped test.

## 3. Green

- [x] 3.1 Add only `PhoneService.Status` using existing `FindPhoneUser` and mask logic; return bound/unbound or sanitized unavailable without provider, transaction or write calls; rerun 2.1 unchanged to Green.
  - Evidence: the minimal service method calls only `FindPhoneUser`, validates the current user, reuses `maskedBinding` for bound data, and returns explicit unbound without provider/Bind calls; the exact 2.1 command Green/Refactor results are recorded above.
- [x] 3.2 Register only `GET /api/v1/me/primary-phone` in existing `PhoneHandler.RegisterRoutes`, reuse exact Bearer authentication, reject non-empty body without Content-Type requirements, emit exact nullable representations/errors and set `Cache-Control: no-store` before every handler exit; rerun 2.1 and 2.2 unchanged to Green.
  - Evidence: the final production registration is one GET line in the existing handler. Exact focused identity, router and smoke commands pass with the approved body/auth/nullable/no-store contract and unchanged POST/anonymous routes; the production diff does not touch router/main.
- [x] 3.3 Complete the real-MySQL status matrix and dedicated runner selection without changing migration/provider/repository/router/main code; rerun 2.3 unchanged to Green and prove user/session state is unchanged.
  - Green/Refactor evidence: restoring only the read-only `FindPhoneUser`/existing-mask service implementation made the unchanged W3 runner pass (`identity 2.091s`); an unchanged rerun passed again (`identity 1.788s`). The test is non-skipping and covers actual Authenticate/HTTP, bound/unbound, unknown/exact-expiry, two users, closed auth/phone DB handles, zero provider/Bind calls and complete user/session snapshot equality. Migration/provider/repository SQL/router/main bytes remain unchanged.

## 4. Refactor and writer Gate

- [x] 4.1 Remove only local duplication without changing the contract, then rerun the exact focused identity, router, smoke and real-MySQL Red/Green commands; record sanitized Red/Green/Refactor evidence and confirm no provider or write path is reachable.
  - Evidence: final focused reruns all exited `0`: identity `0.445s`, router `0.439s`, `smoke: PASS`, and isolated MySQL W3 `1.750s`. The W3 spy/write-snapshot assertions remain active, proving the GET path cannot reach provider/Bind and leaves users/sessions unchanged.
- [x] 4.2 Run existing POST/session/catalog/menu regressions with `services/api/scripts/miniprogram-phone-integration.sh`, `miniprogram-session-integration.sh`, `menu-integration.sh`, `catalog-integration.sh`, `mysql-integration.sh` and `smoke.sh`; preserve all exact existing public contracts.
  - Evidence: all six existing runners exited `0` against the isolated MySQL marker where applicable: migration `3.874s`; catalog/httpapi `2.329s/1.657s`; menu/catalog/httpapi `2.007s/1.789s/1.661s`; session identity `1.427s`; phone identity `1.759s`; `smoke: PASS`.
- [x] 4.3 Run `test -z "$(gofmt -l services/api)"`, full Go test, full race, vet and build using `GOPROXY=off GOTOOLCHAIN=go1.26.5`; frontend/UI and real WeChat are `N/A/NOT_REQUIRED`, not PASS.
  - Evidence: formatting, full `go test ./services/api/...`, full `go test -race ./services/api/...`, `go vet ./services/api/...` and `go build ./services/api/...` all exited `0`. UI is `UI0`; real WeChat is `NOT_REQUIRED` for this read-only route and is not recorded as PASS.
- [x] 4.4 Run `openspec validate serve-miniprogram-primary-phone-status --strict`, `git diff --check a5728022c8e497947267f1b8db5ff50983c03be9...HEAD`, an untracked-inclusive owned-path audit, read-only byte guards for migration/provider/repository/router/main and a sensitive-field/forbidden-route scan.
  - Evidence: strict validation, base tracked diff-check, per-file untracked diff-check, untracked-inclusive owned-path audit, read-only byte guards and sensitive JSON field/forbidden route scans all exited `0`. Frozen Harness branch/base/merge-base/skill blob/skill SHA-256 checks also match exactly.
- [x] 4.5 Record `ui_level_actual=UI0`, real W3/no-write evidence, `C/T/V/R >= 36` with every dimension at least 8 and hard blockers zero; commit only owned paths, record the exact full candidate SHA and confirm clean worktree/index. No candidate exists until this task succeeds.
  - Evidence: checkpoint records `UI0`, Writer W3/no-write PASS, `C9/T10/V8/R9 = 36` and hard blockers `0`. Only owned paths are staged for one candidate commit; its exact self-referential SHA is recorded in the external Writer handoff after commit together with clean worktree/index proof.

## 5. Independent exact-SHA verification

- [ ] 5.1 After task 4.5 only, a different verifier creates a clean detached worktree at the exact candidate SHA, confirms exact HEAD/clean state/owned paths/read-only guards, and reruns strict, focused service/handler/router, real MySQL status/no-write/isolation/failure matrix, existing regressions, full Go test/race/vet/build and smoke without changing candidate bytes.
- [ ] 5.2 Record exact-SHA PASS or the first sanitized failure. Any code, proposal, design, spec, tasks, base, dependency, acceptance command, rebase, merge or SHA change invalidates the result and returns this same Writer lane to Red/Green/Refactor.

## 6. Authorized integration boundary

- [ ] 6.1 Only after an unexpired exact-SHA independent PASS and separate integration authorization, integrate into current local main; if main advances, form a new candidate and rerun complete writer and independent Gates.
- [ ] 6.2 Verify integrated main contains only accepted owned results and record `INTEGRATED`. Push, PR, archive, deploy, production/external writes and real-platform calls remain separately authorized and are not part of this change.
