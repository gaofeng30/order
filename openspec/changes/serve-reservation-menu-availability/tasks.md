## 1. Boundary and approval

- [x] 1.1 确认 proposal/design/spec/tasks/checkpoint 一致声明：单一 outcome、`gate_type=W3`、`ui_level_target=UI0`、`ui_level_actual=NOT_RUN`、base/owner/owned paths、依赖、`required_external_assets=none`、non-goals 与唯一 ACCEPT/REJECT；运行 `openspec validate serve-reservation-menu-availability --strict`。只有主会话明确批准后才记录 `DRAFT → APPROVED → IMPLEMENTING`。
  - Evidence (Approval, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: NOT_RUN, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: approval, command_or_action: main-session review followed by openspec validate serve-reservation-menu-availability --strict and git diff --check, exit_result: PASS, sanitized_summary: main session explicitly approved the corrected DB-driven contract and lifecycle advanced DRAFT to APPROVED to IMPLEMENTING; strict reported Change is valid and diff check exited zero, artifact_or_environment: approved OpenSpec artifacts in the owned writer worktree, unverified_boundary: no runtime preflight Red implementation W3 or candidate evidence exists yet, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`
- [x] 1.2 IMPLEMENTING 开始前确认 main 仍包含 `adopt-0818-prd-baseline@babd1ef662811e3df6a75aa28995268352531438`，本 branch/worktree HEAD 与 base 一致且 clean，shared migration/router/main 路径没有其他 Order writer；任一不满足先停止并重新划分 ownership。
  - Evidence (Implementation entry, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: NOT_RUN, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: implementing-entry, command_or_action: git rev-parse HEAD; git rev-parse main; git merge-base --is-ancestor base HEAD; git status --short --branch; dependency artifact and git history inspection, exit_result: PASS, sanitized_summary: HEAD and main equal the frozen base containing the dependency candidate bytes; branch is the declared writer branch, index and HEAD contain no business changes, working changes are limited to owned change artifacts, and the main session confirmed no parallel Order writer for shared migration router main ownership, artifact_or_environment: /Users/vivix/.codex/worktrees/order-serve-reservation-menu-availability.Writer, unverified_boundary: dependency change-local checkpoint remains historically stale at CANDIDATE so INTEGRATED is the explicit orchestration fact supplied by main session and current main containment, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`

## 2. Writer-managed MySQL W3 preflight

- [x] 2.1 仅在批准 apply 后、任何 Red 前，按 canonical `mysql-persistence-foundation` 建立或核验 writer-managed `order-mysql-w3`：锁定 Colima profile、精确 MySQL 8.0.x image/digest、`linux/arm64`、loopback 随机端口、0600 临时随机凭据与安全 cleanup target。DRAFT 的当前状态保持 `runtime=NOT_ESTABLISHED`、`real_mysql=NOT_RUN`，不记 `BLOCKED_EXTERNAL`。
  - Evidence (Runtime establishment, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: NOT_RUN, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: pre-red-runtime, command_or_action: brew and Colima identity checks; Docker Official Registry tag and manifest enumeration; exact-digest pull run and sanitized inspect, exit_result: PASS, sanitized_summary: Colima 0.10.3 stable bottle a9dfd1fa...381b8; dedicated aarch64 VZ Docker profile has 2 CPU 4 GiB 10 GiB no host mount and no network address; registry latest remains 8.0.46-oraclelinux9 with manifest 7dcddc01...8ae2b and arm64 digest 213bbfaf...2458e; healthy exact-digest container uses loopback random port, 0600 random credential file, noexec nosuid 1 GiB tmpfs, zero mounts, MySQL 8.0.46 and zero owned schema residue, artifact_or_environment: writer-owned order-mysql-w3 disposable local runtime, unverified_boundary: menu behavior and migrations have not entered Red, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`
- [x] 2.2 以不打印 secret/DSN 的结构化环境核验 `ORDER_TEST_MYSQL_INSTANCE=order-mysql-w3`、`ORDER_TEST_MYSQL_ISOLATED=YES` 与必需字段；运行现有 `GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/mysql-integration.sh` 证明 foundation runner 与真实 MySQL 8.0 正常。失败记首个非敏感错误并由当前 writer 修复，不得进入 Red。
  - Evidence (Foundation preflight, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: NOT_RUN, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: pre-red-runtime, command_or_action: GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/mysql-integration.sh, exit_result: PASS after one minimal runtime-env repair, sanitized_summary: first attempt failed at database.Open with invalid_connection_field because TLS value false is outside the existing contract; changing only the temporary env field to disabled made the identical command pass internal migrate race in 3.673 seconds without printing secrets or DSN, artifact_or_environment: order-mysql-w3 structured env and real MySQL 8.0.46, unverified_boundary: foundation PASS is not reservation-menu PASS, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`

## 3. Red

- [x] 3.1 只新增 `internal/menu` 的配置校验、日期/时间选择、cutoff、handler JSON/错误、固定有界 repository 查询/fold 单元契约测试，以及 router 对 `/api/v1/menu`、空 `/menu`、405 与旧 catalog 的契约测试；覆盖明显非法请求 DB 前 400、配置判定的非离散 time 400、负数/跨日/非零秒/缺失/重复/重叠/其他非法配置 503 和无 fallback，不得先写生产实现。
  - Evidence (Config/selection Red, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: NOT_RUN, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: red-config-selection, command_or_action: GOPROXY=off GOTOOLCHAIN=go1.26.5 go test ./services/api/internal/menu -run ^(TestMealConfiguration|TestSelection).* -count=1, exit_result: FAIL, sanitized_summary: focused package failed to compile at undefined MealPeriodRecord and ResolveMeal after tests froze non-default DB configuration discrete endpoints cutoff and invalid stored-data fail-closed cases; no production menu file existed, artifact_or_environment: internal/menu selection contract tests with preflighted writer runtime retained, unverified_boundary: migrations repository HTTP and real menu W3 not yet tested, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`
  - Evidence (Config/selection Green and Refactor, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: NOT_RUN, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: green-refactor-config-selection, command_or_action: same focused go test command before and after naming and formatting refactor, exit_result: PASS then PASS, sanitized_summary: minimum validator and resolver read supplied records with no defaults, enforced exactly lunch and dinner plus same-day minute values interval ordering alignment and closed-range non-overlap, selected non-default discrete points and computed cutoff orderability; refactor retained identical passing command, artifact_or_environment: services/api/internal/menu selection implementation and tests, unverified_boundary: migrations repository HTTP and real menu W3 remain, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`
  - Evidence (Repository/handler/router Red, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: NOT_RUN, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: red-menu-read-http, command_or_action: component-focused TestMenuRepository then TestMenuHandler then TestMenuRoutes commands matching the frozen combined regex, exit_result: FAIL each, sanitized_summary: repository Red failed on absent NewRepository Category Product; handler Red failed on absent NewHandler; router Red failed because NewRouter accepted only catalog and every four-argument caller failed compilation; tests had frozen query count sold-out fold errors exact JSON DB-before-validation and versioned-only route facts first, artifact_or_environment: owned internal menu and router contract tests, unverified_boundary: real integrated menu W3 remained until the new script ran, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`
- [x] 3.2 只新增 v4–v7 migration 形状、v3→v7、初始两行、TIME 分钟点 CHECK、repeat、主外键、非默认合法配置、非法配置、D/D+1、cutoff 与 catalog 回归的真实 MySQL 测试和 `scripts/menu-integration.sh`；运行 `GOPROXY=off GOTOOLCHAIN=go1.26.5 go test ./services/api/internal/menu ./services/api/internal/httpapi ./services/api/migrations -run '^(TestMealConfiguration|TestSelection|TestMenuHandler|TestMenuRepository|TestMenuRoutes|TestEmbeddedMigrationChain).*' -count=1`，必须因目标能力缺失得到非零 Red，并记录首个决定性失败。mock/skip 不得算 W3 Red。
  - Evidence (Migration static Red, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: NOT_RUN, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: red-migrations-static, command_or_action: focused migrations and catalog migration-contract go tests, exit_result: FAIL, sanitized_summary: embedded chain reported 3 migrations instead of exact v1-v7 and every v4-v7 file was absent; v1-v3 remained untouched, artifact_or_environment: owned migration contract tests, unverified_boundary: real MySQL schema was not yet claimed by this failure, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`
  - Evidence (Migration real W3 Red, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: NOT_RUN, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: red-migrations-real, command_or_action: GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/catalog-integration.sh on order-mysql-w3, exit_result: FAIL, sanitized_summary: real isolated MySQL integration stopped at migration count 3 want 7 after tests froze v1-to-v7 repeat schema initial rows CHECK primary-key and RESTRICT assertions, artifact_or_environment: exact-digest MySQL 8.0.46 writer runtime with owned random schema cleanup, unverified_boundary: repository menu HTTP and non-default runtime config remain, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`
  - Evidence (Full focused Green, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: NOT_RUN, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: green-focused, command_or_action: exact combined focused command recorded in task 3.2, exit_result: PASS, sanitized_summary: menu config selection handler repository routes and embedded v1-v7 chain passed across menu httpapi and migrations packages, artifact_or_environment: local Go tests after component Red Green Refactor cycles, unverified_boundary: final full race vet build guards and candidate not yet run, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`

## 4. Green

- [x] 4.1 新增连续单语句 v4–v7 migration：products `meal_period` 默认 `all`；仅 lunch/dinner 的 `meal_periods` cutoff/start/end/interval 表及同营业日分钟点、interval、基本顺序 CHECK；名称不含 `seed` 的单 INSERT 初始两行；以及 `(service_date,product_id)` 主键、RESTRICT 外键的 `product_sold_out_dates`。更新 owned migration/catalog/foundation 测试期望，不修改 v1–v3 bytes。
  - Evidence (Migration Green and Refactor, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: NOT_RUN, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: green-refactor-migrations, command_or_action: focused static command then foundation mysql-integration.sh and catalog-integration.sh twice on the same runtime, exit_result: PASS, sanitized_summary: exact single-statement v4-v7 chain loaded and applied to real MySQL 8.0.46; repeat was zero-write, schema columns indexes initial lunch and dinner minute CHECK invalid writes sold-out date uniqueness and RESTRICT behavior passed; first Green test expected lexical ENUM order and was minimally corrected to MySQL declaration order before same-command PASS; final static foundation and catalog rerun all passed, artifact_or_environment: owned migrations and isolated random MySQL schemas with zero retained test data, unverified_boundary: menu repository HTTP and new menu integration script remain, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`
- [x] 4.2 实现 `services/api/internal/menu/**`：一次时钟快照；严格 query/date 预检；完整两行配置及 `00:00..23:59`、秒=0、interval/顺序/整除/重叠校验；由当前 DB 配置生成离散点与 cutoff；稳定 400/503；一次配置查询加一次菜单 SQL 的固定有界读取；普通价响应；以及 `product.orderable = meal.orderable && !sold_out`。不得内置时间 fallback 或 N+1。
  - Evidence (Menu read Green and Refactor, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: NOT_RUN, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: green-refactor-menu-read, command_or_action: same component-focused repository and handler commands before and after narrow naming response-fold refactors, exit_result: PASS then PASS, sanitized_summary: exactly one complete config query precedes one selected-date menu query, no product query occurs for invalid time or config, one clock snapshot drives today tomorrow cutoff, stable JSON and orderable conjunction passed with no default schedule, artifact_or_environment: internal/menu implementation and scripted reader SQL tests, unverified_boundary: final writer Gate and independent verification remain, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`
- [x] 4.3 只在 `internal/httpapi/router.go` 与 `cmd/order-api/main.go` 装配新 handler 和唯一 `/api/v1/menu`；不注册 `/menu`，不修改 catalog 生产代码或 JSON。
  - Evidence (Router Green and Refactor, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: NOT_RUN, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: green-refactor-router, command_or_action: GOPROXY=off GOTOOLCHAIN=go1.26.5 go test ./services/api/internal/httpapi -run ^TestMenuRoutes.* -count=1 run twice, exit_result: PASS then PASS, sanitized_summary: explicit catalog plus menu wiring registered only GET /api/v1/menu; business-only and child paths stayed empty 404, POST stayed empty 405 and none reached the menu reader; catalog remained exact, artifact_or_environment: owned router main and tests, unverified_boundary: full API regression and exact candidate remain, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`
- [x] 4.4 重跑 3.2 完全相同的 focused 命令取得 Green，其中 reader double 证明负数/跨日/非零秒与重复配置稳定 503；再运行 `GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/menu-integration.sh`，在真实 owned MySQL 8.0 证明 v3→v7/repeat/schema/history/初始行、分钟点 CHECK 与重复键拒绝、非默认合法配置被实际读取、可持久化的缺失/重叠/不对齐配置 503、D/D+1/cutoff/catalog 且随机 schema 清理成功。
  - Evidence (Menu real W3 Green, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: NOT_RUN, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: green-real-menu, command_or_action: GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/menu-integration.sh, exit_result: PASS, sanitized_summary: executable owned script passed menu catalog and httpapi race suites on exact MySQL 8.0.46; real API read non-default lunch 10:45 and 20-minute points, filtered all plus selected meal, omitted hidden and unmatched categories, kept D sold-out browseable and isolated D+1, returned stable 503 for persisted missing overlap and misalignment, upgraded v3 to v7 and repeated zero-write; first run only corrected a test expectation because now 10:30 is before cutoff 10:45, artifact_or_environment: order-mysql-w3 random owned schemas cleaned by test, unverified_boundary: full writer Gate independent verification integration archive push and deploy remain, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`

## 5. Refactor

- [x] 5.1 在不改变 JSON、时间表、schema 或 SQL 语义下去重命名与测试 helper，重跑 3.2 完全相同的 focused 命令和 `services/api/scripts/menu-integration.sh`；确认并发业务写、事务业务恢复、UI 均因无对应路径为 `N/A`，不得写 PASS。
  - Evidence (Refactor, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: UI0, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: refactor, command_or_action: exact focused command and menu-integration.sh after narrow naming response-fold and fixture-helper cleanup, exit_result: PASS, sanitized_summary: focused menu httpapi migrations passed and real menu catalog httpapi race suites passed again; existing v3 products default to all, sold-out orphan and duplicate remain rejected, non-default config plus sold-out facts do not alter catalog JSON; concurrent business writes transaction business recovery and UI are N/A because no such paths exist, artifact_or_environment: Go packages plus exact-digest order-mysql-w3 random schemas, unverified_boundary: N/A dimensions are not PASS and full writer Gate remains, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`
- [x] 5.2 运行 `GOPROXY=off GOTOOLCHAIN=go1.26.5 go test -race ./services/api/internal/menu ./services/api/internal/httpapi ./services/api/internal/catalog ./services/api/migrations -count=1`，覆盖 handler/router、repository、迁移与 catalog 精确回归。
  - Evidence (Focused race, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: UI0, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: not-yet-created, phase: refactor-race, command_or_action: GOPROXY=off GOTOOLCHAIN=go1.26.5 go test -race ./services/api/internal/menu ./services/api/internal/httpapi ./services/api/internal/catalog ./services/api/migrations -count=1 -timeout=3m with owned env, exit_result: PASS, sanitized_summary: menu httpapi catalog and migrations packages all passed race while real integration environment was present, artifact_or_environment: writer worktree and order-mysql-w3, unverified_boundary: full services API gate and candidate still pending, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`

## 6. Writer gate

- [x] 6.1 依次运行并记录：`openspec validate serve-reservation-menu-availability --strict`；`git diff --check babd1ef662811e3df6a75aa28995268352531438...HEAD`；包含 untracked files 的 owned-path audit；`git diff --exit-code babd1ef662811e3df6a75aa28995268352531438 -- services/api/internal/catalog/model.go services/api/internal/catalog/repository.go services/api/internal/catalog/handler.go services/api/internal/catalog/repository_test.go services/api/internal/catalog/handler_test.go services/api/migrations/000001_create_schema_migrations.sql services/api/migrations/000002_create_categories.sql services/api/migrations/000003_create_products.sql`。
  - Evidence (Static writer Gate, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: UI0, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: external-post-commit, phase: writer-static, command_or_action: strict; working-tree diff and whitespace checks; untracked-aware owned-path audit; catalog production plus v1-v3 byte guard; gofmt; route and script-mode checks, exit_result: PASS, sanitized_summary: strict valid, every changed or untracked path is owned, catalog production and immutable migrations match base, one source registration exists for /api/v1/menu, no source registration exists for /menu and menu integration mode is 755; first shell attempt used zsh special variable path and lost PATH, task-specific changed_path fixed only the checker and identical rerun passed, artifact_or_environment: writer worktree against frozen base, unverified_boundary: exact base-to-candidate diff check is repeated immediately after the immutable commit and independent verification remains, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`
- [x] 6.2 依次运行并记录：`test -z "$(gofmt -l services/api)"`；`GOPROXY=off GOTOOLCHAIN=go1.26.5 go test ./services/api/...`；`GOPROXY=off GOTOOLCHAIN=go1.26.5 go test -race ./services/api/...`；`GOPROXY=off GOTOOLCHAIN=go1.26.5 go vet ./services/api/...`；`GOPROXY=off GOTOOLCHAIN=go1.26.5 go build ./services/api/...`；`GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/smoke.sh`；现有和新 MySQL integration 两个脚本。前端/JS/微信/checkout 因不在影响面记 `N/A`。
  - Evidence (Executable writer Gate, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: UI0, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: external-post-commit, phase: writer-executable, command_or_action: full services API test then race vet build smoke mysql-integration catalog-integration menu-integration in frozen order, exit_result: PASS, sanitized_summary: all Go packages passed ordinary and race suites with the real owned environment present; vet and build exited zero; smoke passed; foundation catalog and menu scripts passed; container remained healthy exact digest loopback zero mounts with owned schema residue zero, artifact_or_environment: Go 1.26.5 plus order-mysql-w3 MySQL 8.0.46, unverified_boundary: frontend JS WeChat checkout concurrent business write and transaction recovery are N/A not PASS; independent exact-SHA verification remains, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`
- [x] 6.3 记录真实 Red/Green/Refactor/writer 统一证据，确认旧 catalog exact JSON、无第二路由、日志无 query/secret、worktree diff 只含 owned paths，且 `C/T/V/R >= 36`、每项 `>=8`、hard blockers `0` 后只提交本 change，绑定完整 `candidate_sha` 并确认 worktree/index clean。
  - Evidence (Candidate preparation, 2026-08-20): `{ change: serve-reservation-menu-availability, gate_type: W3, ui_level_target: UI0, ui_level_actual: UI0, base_sha: babd1ef662811e3df6a75aa28995268352531438, candidate_sha: external-post-commit, phase: writer-candidate, command_or_action: reconcile RGR evidence and final owned diff before one Chinese commit, exit_result: PASS, sanitized_summary: old catalog exact JSON passed under non-default meal config and date sold-out fixture; access log retains path without raw query and stable errors contain no underlying values; C9 T10 V8 R9 equals 36 with each at least 8 and hard blockers zero, artifact_or_environment: complete owned candidate tree, unverified_boundary: V is capped at 8 and full candidate SHA plus clean index are bound only after commit; no integration archive push PR or deploy, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`

每项证据使用：`change/gate_type/ui_level_target/ui_level_actual/base_sha/candidate_sha/phase/command_or_action/exit_result/sanitized_summary/artifact_or_environment/unverified_boundary/external_asset`；普通证据只保留首个脱敏决定性结果。`external_asset.owner/missing/recovery` 固定 `N/A`；writer-managed runtime 状态单独记录，不能冒充 external asset。

## 7. Independent exact-SHA verification

- [ ] 7.1 verifier 在另一 clean detached worktree 检出完整 candidate SHA，确认 HEAD 精确匹配、worktree clean、base/dependency/owned paths/只读 bytes 一致，从头重跑 3.2、4.4、5.2 与全部 writer Gate；不得修改 candidate bytes。
- [ ] 7.2 verifier 只返回 exact-SHA PASS 或首个真实 FAIL 与未验证边界；任一 artifact、验收命令、base、dependency、配置 shape、schema、响应字段或 candidate 变化使旧验证立即失效，测试配置数据变化使对应运行证据失效。writer 与 verifier 均真实 W3 PASS 后，按 canonical foundation 只清理 `order-mysql-w3` 容器/profile/data disk 并确认不存在。

## 8. Authorized integration boundary

- [ ] 8.1 仅在依赖仍为当前 main 的 `INTEGRATED`、candidate exact-SHA 获未失效 independent PASS、required review 与全部 Gate 满足且取得单独集成授权后，才可集成本地 main；main 推进则重建 candidate 并重验。
- [ ] 8.2 集成后只核对 main 包含已验证 bytes 和 owned paths并记录 `INTEGRATED`；archive、push、PR、deploy、生产写入与任何外部系统操作均未授权。
