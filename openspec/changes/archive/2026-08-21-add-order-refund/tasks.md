## 1. 门禁（Red）

- [x] 1.1 写 `checks/check_refund.js` 十二项
- [x] 1.2 在 base_sha `20ddc15` 树上确认十一项红（跨区间退款净额一项为非回归守卫，本就应绿）

## 2. 纠正部分退款（上一 change 引入的错误）

- [x] 2.1 种子那笔 `已退款` 由 1200 改回全额 3800
- [x] 2.2 契约层去掉 `partial` 标记
- [x] 2.3 财务页去掉「部分退款 / 实付」渲染
- [x] 2.4 规格中「部分退款是合法情形」一句改为跨区间退款的理由，并加「金额恒等于订单实付」

## 3. 契约层

- [x] 3.1 `today()` 单一下发营业日
- [x] 3.2 `currentAccount()` 取当前登录的启用主账号
- [x] 3.3 `refundOrder(id, reason)`：四态可退、无金额入参、原因必填、只到 `退款中`、重复被拒
- [x] 3.4 `canRefund(status)` 供页面判定按钮显隐
- [x] 3.5 `listOrders(lane, { uncollected })` 与 `uncollectedCount()`
- [x] 3.6 门禁在候选树 `REFUND_GATE=PASS`

## 4. 种子

- [x] 4.1 补一笔营业日已过仍为 `待取餐` 的订单（08-20 取餐，未核销）
- [x] 4.2 已归档的 `check_order_model.js` 在新种子上仍全过

## 5. 页面

- [x] 5.1 订单详情页脚加「发起退款」，按 `canRefund` 显隐
- [x] 5.2 退款确认层：将退金额（只读）、操作人、必填原因、不可撤销后果
- [x] 5.3 退款成功跳转 `退款中` 泳道并选中该单
- [x] 5.4 `待取餐` 泳道加「未取餐」开关，带条数角标
- [x] 5.5 切泳道与退款跳转两处都清除 `uncollected`
- [x] 5.6 财务页营业日改取 `Api.today()`
- [x] 5.7 复用既有 `Modal.open / segs / seg / fld-lb / btn--danger`，不新造 CSS

## 6. 规格

- [x] 6.1 两条 MODIFIED、两条 ADDED requirement

## 7. UI1 证据

- [x] 7.1 `python3 -m http.server 8094` 起站
- [x] 7.2 `待取餐` 泳道 3 单，启用「未取餐」后只剩 08-20 那单
- [x] 7.3 切到其他泳道后「未取餐」开关消失
- [x] 7.4 退款层展示「¥51.00 全额，不可修改」「操作人 高特」，无金额输入框
- [x] 7.5 空原因提交被拒，弹层保持打开
- [x] 7.6 填原因后确认：跳转 `退款中 2` 并选中该单，详情状态为 `退款中`
- [x] 7.7 退款后页脚只剩「打印小票」—— 不可再退，也无撤销
- [x] 7.8 `已退款` 单的页脚同样只有「打印小票」
- [x] 7.9 财务页即时显示该退款：操作人 高特、原因、状态 `退款中`
- [x] 7.10 汇总 510.80 − 62.00 = 448.80，「2 笔未到账」
- [x] 7.11 全部退款记录金额均等于原订单实付
- [x] 7.12 控制台无 error

## 8. 回归

- [x] 8.1 小程序 `npm test` 65/65
- [x] 8.2 历史归档门禁：唯一新增失败为 `check_finance.js` 依赖部分退款种子的那一项，属纠错，断言已由本门禁接管（见 proposal「与已归档门禁的冲突」）；其余失败集合与 base_sha 树完全一致

## 9. 交付

- [x] 9.1 提交
- [x] 9.2 clean worktree 独立验证
- [x] 9.3 合入 main、应用 delta、归档、推送

- [x] 9.4 补第三条 MODIFIED：`align-order-model` 的「退款金额 MUST NOT 超过订单实付 —— 部分退款是合法情形」同样与 §7.7 冲突，一并改为「恒等于订单实付」，并加断言防止生效 spec 里再出现该表述
