## 1. 门禁（Red）

- [x] 1.1 写 `checks/check_pending.js` 十二项
- [x] 1.2 在 base_sha `0ad7fbe` 树上确认十一项红（JS 解析为非回归守卫，本就应绿）

## 2. 种子

- [x] 2.1 新增 `PENDING_PAYMENTS` 三条，覆盖 §7.3 的三种原因
- [x] 2.2 「商品已下架」那条指向本就下架的 `p007`，使解除原因后重试可被走通
- [x] 2.3 三条的结算恒等式（小计 / 折后 / 实付）全部成立
- [x] 2.4 已归档的 `check_order_model.js` 在新种子上仍全过

## 3. 契约层

- [x] 3.1 `listPendingPayments` / `pendingPaymentCount`
- [x] 3.2 `blockingReason(p)` 重新判定阻塞原因，覆盖删除 / 下架 / 售罄 / 时间已过 / 校验未过
- [x] 3.3 `rebuildOrder(id)`：沿用支付事实，按 §7.8 分配当日唯一取餐号，按 §7.4 定状态，成功后条目出列
- [x] 3.4 `refundPendingPayment(id, reason)`：无金额入参、原因必填、只到 `退款中`、记录操作人、不生成订单
- [x] 3.5 `_refunds` 纳入被作废条目，带 `fromPending` 标记与可回溯字段
- [x] 3.6 汇总新增 `unbuiltCount / unbuiltAmount`，与 `pendingCount` 区分
- [x] 3.7 门禁在候选树 `PENDING_GATE=PASS`

## 4. 页面

- [x] 4.1 `pages/pending.js` 八列表格与两个动作
- [x] 4.2 建单确认层预判阻塞原因，原因仍在时不渲染确认按钮
- [x] 4.3 作废确认层：全额只读、操作人、必填原因、不可撤销后果
- [x] 4.4 页面说明条目来源与「只有自动补建也失败的才在这里」
- [x] 4.5 财务页净额卡加差额旁注，底部口径补成因与消解方式
- [x] 4.6 `app.js`「经营」组加入口；`index.html` 挂载
- [x] 4.7 复用既有 `Modal.open / Table / imp-note / btn--danger`，不新造 CSS

## 5. 文档与规格

- [x] 5.1 四条 ADDED requirement
- [x] 5.2 PRD §16.5 记录扫描链路仍属后端缺口

## 6. UI1 证据

- [x] 6.1 `python3 -m http.server 8095` 起站，三条待处理条目渲染完整
- [x] 6.2 商品仍下架时建单：确认层给出「请先在菜品管理重新上架」，无确认按钮
- [x] 6.3 取餐时间已过时建单：给出「已过，无法排产。请退款作废」，无确认按钮
- [x] 6.4 数据校验不通过时建单：同样被拒，无确认按钮
- [x] 6.5 上架 `p007` 后重试：建单成功，取餐号 `0189`（当日最大 0188 加一），状态 `已预约`，金额 ¥40.00，操作人 高特
- [x] 6.6 该取餐日期内 12 个取餐号无重复
- [x] 6.7 建单后条目离开列表（契约、store、DOM 三处一致）
- [x] 6.8 作废层无金额输入框，空原因被拒且弹层保持打开
- [x] 6.9 作废成功后不生成订单，且以预支付单号进入退款台账并标 `[待处理]`
- [x] 6.10 财务页净额卡标注「另有 2 笔已收款未建单 ¥66.00，未计入」
- [x] 6.11 两条处理完毕后：实收 550.80、退款 62.00、净额 488.80，差额归零
- [x] 6.12 净额等式在两种状态下均成立；控制台无 error

## 7. 回归

- [x] 7.1 小程序 `npm test` 65/65
- [x] 7.2 历史归档门禁失败集合与 base_sha 树逐行一致，无新增

## 8. 交付

- [x] 8.1 提交
- [x] 8.2 clean worktree 独立验证
- [x] 8.3 合入 main、应用 delta、归档、推送
