## 1. Boundary and approval

- [x] 1.1 Freeze the runner base, record owner/worktree/base SHA, W1/UI0 classification, dependencies, external assets, non-goals and the user's explicit approval; run `openspec validate add-lightweight-harness-loop --strict` before implementation.
  - Evidence (Planning, 2026-08-17): `{ change: add-lightweight-harness-loop, gate_type: W1, ui_level_target: UI0, ui_level_actual: NOT_RUN, base_sha: d817aeb3ac5de29d2695ed17ed6277b737ba3ee8, candidate_sha: not-yet-created, phase: planning, command_or_action: openspec validate add-lightweight-harness-loop --strict, exit_result: PASS, sanitized_summary: proposal design delta spec tasks ownership dependencies non-goals local-only assets and one acceptance verdict are complete; user explicitly authorized the minimal faster standardized self-evolving closed loop, artifact_or_environment: openspec/changes/add-lightweight-harness-loop and frozen runner checkpoint, unverified_boundary: no executable harness or implementation evidence exists yet, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`

## 2. Red

- [x] 2.1 Add focused Python CLI tests that fail because `./tools/harness` is absent, covering human/JSON status, all four derived task states, missing/invalid checkpoints, illegal lifecycle jumps, legacy bootstrap ancestry, atomic shared-worktree state and immutable observation validation.
  - Evidence (Red setup, 2026-08-17): `tools/tests/test_harness.py` defines 11 standard-library tests; Python 3.9 compatibility was corrected before accepting Red, and implementation-dependent cases remain frozen behind only the missing-entrypoint precondition.
- [x] 2.2 Run the focused suite against the unimplemented repository and record the first decisive missing-entrypoint Red without weakening assertions.
  - Evidence (Red, 2026-08-17): `{ change: add-lightweight-harness-loop, gate_type: W1, ui_level_target: UI0, ui_level_actual: UI0, base_sha: d817aeb3ac5de29d2695ed17ed6277b737ba3ee8, candidate_sha: not-yet-created, phase: red, command_or_action: /usr/bin/python3 -m unittest -v tools.tests.test_harness, exit_result: FAIL, sanitized_summary: 1 decisive failure tools/harness is missing and 10 implementation-dependent tests skipped, artifact_or_environment: Python 3.9 local CLI fixture, unverified_boundary: no harness behavior passes yet, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`

## 3. Green

- [x] 3.1 Implement the zero-dependency `status` and `check` paths with repository discovery, active-change/task parsing, Git common-dir state loading, fail-closed validation and `WHAT / WHY / FIX` errors.
  - Evidence (Green, 2026-08-17): `status` human/JSON, missing-state `UNKNOWN`, derived task states and fail-closed `check` passed the focused CLI fixtures without external dependencies.
- [x] 3.2 Implement `checkpoint` with the existing seven-state transition graph, evidence/next requirements, task/Git/ancestry checks, explicit legacy bootstrap and lock-protected atomic writes.
  - Evidence (Green, 2026-08-17): illegal DRAFT→IMPLEMENTING, invalid task, invalid SHA and held-lock fixtures returned non-zero without changing prior state; explicit evidence-backed legacy INTEGRATED bootstrap passed only with valid ancestry.
- [x] 3.3 Implement append-only `observe` with four immutable classes, unique IDs, required evidence/next, lock protection and no runner/spec mutation.
  - Evidence (Green, 2026-08-17): valid checker observation appended once and appeared in JSON status; invalid class and corrupt JSONL returned non-zero and preserved prior bytes.
- [x] 3.4 Run the same focused suite to Green, then initialize the current repository's local state for the three legacy integrated changes and this implementing change using verifiable Git evidence.
  - Evidence (Green, 2026-08-17): `{ change: add-lightweight-harness-loop, gate_type: W1, ui_level_target: UI0, ui_level_actual: UI0, base_sha: d817aeb3ac5de29d2695ed17ed6277b737ba3ee8, candidate_sha: not-yet-created, phase: green, command_or_action: /usr/bin/python3 -m unittest -v tools.tests.test_harness and four ./tools/harness checkpoint sequences, exit_result: PASS, sanitized_summary: 11 focused tests passed; three legacy changes bootstrapped only from full ancestor SHAs; this change advanced DRAFT to APPROVED to IMPLEMENTING; harness check passed 4 active changes, artifact_or_environment: Python 3.9 plus shared /Users/vivix/order/.git/codex-harness local index, unverified_boundary: local ledger is routing state not lifecycle or verifier proof; governance wiring and full regressions remain, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`

## 4. Refactor and governance wiring

- [x] 4.1 Refactor only for clarity and rerun the focused suite; keep one executable, standard-library-only implementation and four subcommands.
  - Evidence (Refactor, 2026-08-17): one executable `tools/harness` remains standard-library-only with exactly `{status,check,checkpoint,observe}`; the expanded 14-test suite passed after the focused implementation fix.
- [x] 4.2 Add the minimal root `AGENTS.md` and `order-run-loop` entrypoint rules; keep the validated delta as the future canonical control-plane input without pre-editing the canonical spec before integration/archive.
  - Evidence (Refactor, 2026-08-17): root governance adds four concise start/checkpoint/check/observe rules; `order-run-loop` adds one thin entry section and reuses the existing ledger/Gates; canonical spec is unchanged until authorized integration/archive.
- [x] 4.3 Add and run non-weakening, owned-path and fresh-session checks proving stage skills, exact-SHA invalidation, lane limits, authorization boundaries, receipt rules and observation-only self-evolution remain intact.
  - Evidence (Refactor, 2026-08-17): `{ change: add-lightweight-harness-loop, gate_type: W1, ui_level_target: UI0, ui_level_actual: UI0, base_sha: d817aeb3ac5de29d2695ed17ed6277b737ba3ee8, candidate_sha: not-yet-created, phase: refactor, command_or_action: 14-test focused suite; skill quick_validate; stage-skill byte diff; runner invariant search, exit_result: PASS, sanitized_summary: shared-worktree fresh-session fixture, four-command thin layer, WHAT-WHY-FIX failures, stage handler bytes, two-lane limit, exact SHA, third-fingerprint stop, BLOCKED_EXTERNAL and receipt rules remained present; one environment observation remained promotion NOT_RUN, artifact_or_environment: local Python/Git fixtures and current runner package, unverified_boundary: full historical receipt runner requires a clean candidate and includes legacy tests tied to an already archived active path; current receipt CLI is deferred to the clean writer/verifier Gate and no historical failure is called PASS, external_asset: { owner: N/A, missing: N/A, recovery: rerun applicable receipt command on clean candidate } }`

## 5. Writer candidate

- [x] 5.1 Run `./tools/harness check`, focused Python tests, strict OpenSpec, diff/owned-path checks, Skill validation, lifecycle receipt regressions and all workflow-quality repository commands required for W1/UI0.
  - Evidence (Writer, 2026-08-17): `{ change: add-lightweight-harness-loop, gate_type: W1, ui_level_target: UI0, ui_level_actual: UI0, base_sha: d817aeb3ac5de29d2695ed17ed6277b737ba3ee8, candidate_sha: not-yet-created, phase: writer, command_or_action: harness check; Python 14 tests; change/all strict; diff and stage-skill byte checks; skill quick_validate; Go test/race/vet/build/smoke; JS syntax; JSON parse; miniprogram UI1, exit_result: PASS, sanitized_summary: harness 4 active changes and 1 observation consistent; Python 14/14; OpenSpec 13/13; Go all PASS including race and smoke; JSON 44; UI1 13/13; exact command outputs retained in current writer session, artifact_or_environment: local writer on branch codex/add-lightweight-harness-loop, unverified_boundary: lifecycle receipt CLI requires a clean committed candidate and is rerun immediately after commit; broad historical unit discovery is N/A because its retired bootstrap fixture expects an already archived active path and separately requires clean, external_asset: { owner: N/A, missing: N/A, recovery: run the current receipt CLI on the clean exact candidate } }`
- [x] 5.2 Record sanitized Red/Green/Refactor/writer evidence, C/T/V/R self-score with no hard blocker, checkpoint the change as `CANDIDATE`, commit only owned paths and return the exact full candidate SHA with a clean worktree.
  - Evidence (Writer candidate preparation, 2026-08-17): `{ change: add-lightweight-harness-loop, gate_type: W1, ui_level_target: UI0, ui_level_actual: UI0, base_sha: d817aeb3ac5de29d2695ed17ed6277b737ba3ee8, candidate_sha: external-post-commit, phase: writer, command_or_action: owned-path audit and candidate commit preparation, exit_result: PASS, sanitized_summary: C9/T10/V8/R9=36; hard blockers 0; only declared governance change, runner skill, OpenSpec artifacts, harness executable and focused tests are included; local ledger remains non-proof and outside the candidate tree, artifact_or_environment: writer worktree, unverified_boundary: V8 means independent exact-SHA verification is still required; no integration archive push or deploy is authorized, external_asset: { owner: N/A, missing: N/A, recovery: N/A } }`

## 6. Independent verification

- [ ] 6.1 In another clean detached worktree at the exact candidate SHA, start from an empty local harness state, prove `UNKNOWN/NO-GO`, bootstrap only from declared evidence, then rerun every acceptance and regression command without modifying tracked files.
- [ ] 6.2 Record independent PASS/FAIL, exact SHA, command results and remaining limitations; any code/spec/task/base/SHA change invalidates the result.
