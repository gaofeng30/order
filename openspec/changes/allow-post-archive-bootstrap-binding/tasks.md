## 1. Approval and Exact Boundary

- [x] 1.1 Obtain explicit APPROVED for these four strict-valid artifacts; bind independent planner/writer thread `019ffe92-0854-74f2-89b9-918c78f85efe` (`hostId=local`) to this branch/worktree; re-read apply instructions; and verify clean base `44aea3b3f892acf8a6e0537f65f28f33de22cf2f`, frozen runner identity, dependency, and exact owned paths before any implementation edit.
  - Evidence: Main Gate APPROVED after fresh reviewer `019ffed1-b59e-7883-867d-2c0316bfb7ef` reported `APPROVABLE`, P0/P1/P2=`0`; apply instructions and skill reread; strict PASS; branch `codex/allow-post-archive-bootstrap-binding`; tracked diff clean with only the six expected untracked planning/scaffold files.
- [x] 1.2 Reconfirm `d0b70a077bcaa64c401837eb0e9b6f27035210a0` is the direct ancestor of archive `44aea3b3f892acf8a6e0537f65f28f33de22cf2f`, while its archived checkpoint says `independent_verification=NOT_RUN` and tasks 5.x are unchecked. Preserve attempt 1 `FAIL` and fingerprint count `1`. The main Gate read attempt 2 final using thread `019ffeab-ca4c-71f3-aa90-d15283219361` and `hostId=local`; audit-only `wait_cursor` (`wait_threads.afterCursor`) `aef7a56e-afd6-4f74-b5a3-3a5a7e0f8440:80`; exact `d0b70a...`; `INDEPENDENT_VERIFIED`; all required Gates PASS; `C9/T10/V9/R9=37`; hard blockers `0`. This satisfied the historical dependency Gate before Main Gate approval; neither the handoff nor wait cursor may be supplied to or interpreted by the loader.
  - Evidence: `git rev-list --parents -n 1 44aea3b...` returned exactly `44aea3b... d0b70a...`; `git merge-base --is-ancestor d0b70a... 44aea3b...` exited `0`; current PASS pointer remains audit-only.
- [x] 1.3 Reserve semantic labels `C`=repair candidate, `A`=repair archive, `B`=binding-head, and `R`=receipt-head. Every dispatch MUST carry the applicable full 40-hex SHA as a mandatory literal task input; unresolved labels, branch names, moving refs, abbreviated SHAs, and shell SHA variables are rejected before execution.
  - Evidence: approved tasks contain no bare shell SHA variables; current implementation has no candidate/archive/binding/receipt SHA yet.
- [x] 1.4 For every verifier recipe below, the verifier follows `order-verify-change`: create a new clean detached worktree at the dispatcher-provided exact SHA, record its absolute workdir, then execute the listed commands there. Worktree creation is not implicit recipe work and writer worktrees are never reused.
  - Evidence: verifier isolation remains a later exact-candidate Gate; writer state is `IMPLEMENTING`, not independently verified.

## 2. Red: Missing Stage-Aware Admission

- [x] 2.1 Add `test_post_archive_bootstrap_binding.py` with an isolated local `C→A→B` Git history derived from the real base; run `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tools/lifecycle-receipts/tests/test_post_archive_bootstrap_binding.py -k test_exact_later_bootstrap_binding_is_admitted -v` and record the current loader's meaningful failure `bootstrap profile must remain unbound in the candidate`, not a missing-module or fixture failure.
  - Red evidence: after correcting one fixture-only missing historical wrapper, the exact command exited `1` at `profile_runner.py:256` with `ProfileError: bootstrap profile must remain unbound in the candidate`; fingerprint `bootstrap-binding-admission|semantic|valid-post-archive-fourth-treated-as-candidate-unbound|44aea3b|binding-head`, count `2`.
- [x] 2.2 Before production edits, add exactly two candidate-owned Gate outputs: `openspec/changes/allow-post-archive-bootstrap-binding/checks/verify_archive.py` and the complete target bytes at `checks/expected-canonical-loop-engineering-control-plane-spec.md`. Add their fixture cases to existing `tools/lifecycle-receipts/tests/test_post_archive_bootstrap_binding.py`, then run the focused canonical-smuggling case against a minimal parent/R100/path-only scaffold so Red is false acceptance, never a missing module. Preserve negatives for canonical extra/missing/reordered byte, smuggled judge/other path, wrong/non-single parent, merge, incomplete/non-same-relative/non-`R100` rename, wrong HEAD, dirty worktree, mutated moved byte, archive-time executor/registry/wrapper drift, candidate/pre-archive/same-stage binding, duplicate/fifth binding, wrong target/definition/hash/path/blob, profile argv/env/expected/timeout/network/write/output drift, extra-path binding commit, ambiguous ancestry, later binding edit, current source drift, and receipt-supplied commands.
  - Red evidence: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tools/lifecycle-receipts/tests/test_post_archive_bootstrap_binding.py -k test_archive_gate_rejects_extra_canonical_byte -v` exited `1`; the weak Gate incorrectly returned `0` and stdout `archive-gate=PASS\n` for one extra canonical byte. Fingerprint `archive-gate|false-green|canonical-extra-byte-accepted|44aea3b|writer`, count `1`.

## 3. Green: One Exact Post-Archive Bootstrap

- [x] 3.1 Change only `profile_runner.py` to accept either exact historical-three/unbound or exact historical-three plus the one fixed bootstrap target; keep `--all-historical` at three and reject arbitrary counts/profiles.
  - Green evidence: the focused suite accepted the exact later fourth binding and retained the exact candidate-era three; `profile_runner.py --all-historical --json` returned exactly the original three profiles with two `MECHANICAL_PASS`, one `EXPECTED_MECHANICAL_FAIL`, and actor independence `NOT_PROVEN_BY_MECHANICAL_REPLAY` for all three.
- [x] 3.2 Implement exact `C→A→B` Git derivation and the read-only `verify_archive.py`: the fixed direct-argv bootstrap executes checker bytes from exact `C`, and that checker executes runner-validator bytes from exact `C`; the `A` copies are compared subjects only. Accept only direct argv `--repo`, literal full `--candidate`, and literal full `--archive`; require clean `HEAD=A`, sole parent `C`, complete same-relative-path `R100` active-to-one-dated-archive move, only canonical spec `M`, canonical blob at `A` byte-identical to the expected fixture blob at `C`, and registry/binding-registry/wrapper/executor/receipt-judge/frozen-runner blobs identical at `C` and `A`. Success MUST write exactly `archive-gate=PASS\n`; failure MUST exit `1` at the first decisive error. Then require unique later binding-only `B`, no later binding edit, and complete definition/source pinning; preserve immutable historical binding objects and receipt hashes. Do not generate the fixture at `A` or trust mutable CLI output.
  - Green evidence: the exact archive fixture returned exit `0`, stdout exactly `archive-gate=PASS\n`, and empty stderr; byte-extra/missing/reordered, judge, both registries, wrapper, executor, frozen Skill/reference, extra path, parent/merge/rename, HEAD, and dirty variants all failed closed at exit `1`.
- [x] 3.3 Rerun the identical positive and negative focused suite; require the valid later binding Green and every premature/malformed/drift case Green without shell evaluation, mock replacement, assertion weakening, skipped tests, or persisted actor-independence claims.
  - Green/refactor evidence: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tools/lifecycle-receipts/tests/test_post_archive_bootstrap_binding.py -v` ran `23` tests in `8.312s`, all PASS, no skip/mock/shell evaluation; bindings remain exactly three and bootstrap receipt absent in the candidate tree.

## 4. Refactor and Writer Gate

- [x] 4.1 Refactor only duplicated validation helpers, then rerun serially with `PYTHONDONTWRITEBYTECODE=1`: focused `python3 -m unittest tools/lifecycle-receipts/tests/test_post_archive_bootstrap_binding.py -k archive_gate -v`, full `python3 -m unittest discover -s tools/lifecycle-receipts/tests -p 'test_*.py'`, archived forward/runner suites, and existing three historical profile/chain checks; assert candidate bindings remain exactly three/unbound, both exact Gate outputs are committed candidate bytes, and ending status contains no pyc/untracked file.
  - Writer evidence: full receipt discovery `50/50` PASS; archived forward `7/7` PASS; runner contract printed `runner receipt contract PASS`; historical profiles matched the exact three-result matrix; an equivalent temporary clean preview commit made receipt `--chain --json` exit `0` while preserving historical `FAILED`, derived results, and actor `NOT_PROVEN_BY_MECHANICAL_REPLAY`. The dirty pre-commit source correctly failed chain closed and is recorded separately; generated pyc files were precisely removed before this Gate.
- [x] 4.2 Run `openspec validate allow-post-archive-bootstrap-binding --strict`, whitespace, owned/protected/sensitive/no-shell audits, exact local Go format/test/race/vet/build/smoke, and all-app JS/JSON checks; record UI0 actual and UI1+ N/A rather than PASS.
  - Writer evidence: strict printed `Change 'allow-post-archive-bootstrap-binding' is valid`; tracked/untracked whitespace, owned/protected, bare-SHA-variable, sensitive and no-shell audits PASS; gofmt was empty; offline Go test/race/vet/build and smoke PASS; JS syntax was empty and JSON printed `JSON OK 44`. UI0 is actual; UI1+ is N/A, not PASS.
- [x] 4.3 Freeze proposal/design/spec/tasks/checkpoint evidence with writer target `C9/T10/V8/R9=36`, hard blockers `0`; commit only owned paths once, return the literal full repair-candidate SHA to the dispatcher, and leave bootstrap binding/current receipt absent. Candidate writer evidence MUST NOT mark either verifier Gate, archive, binding-head, receipt-head, or Goal0 closure PASS.
  - Candidate boundary: this evidence becomes true atomically in the single candidate-producing commit; because a commit cannot contain its own SHA, the checkpoint binds `candidate_sha` to that commit and the writer handoff returns its literal full value. Tasks 5.x/6.x and every Goal0 outer Gate remain unchecked/`NOT_RUN`; writer V8 does not claim independence.

## 4A. Corrective TDD after rejected candidate

- [x] 4.4 **Red.** Preserve rejected exact candidate `77bdf711575c390e5bc56b2d5eaf3a8f1e6caa64` and independent FAIL pointer `threadId=019ffef3-ac44-7c22-9c4a-062a6f2dbcc4`, `hostId=local`, fingerprint `archive-gate|false-green|A-controlled-runner-overrides-validator|77bdf71|verifier`, count `1`. Add focused local-Git negatives proving that a sole-parent `A` which replaces either the runner validator or the archived checker itself cannot obtain PASS; the runner case MUST reproduce exit `0` and stdout exactly `archive-gate=PASS\n` before the root fix.
  - Red evidence: the runner test exited `1` because the malicious archive Gate itself returned exit `0` with stdout exactly `archive-gate=PASS\n`; this is recurrence count `2` of the verifier fingerprint. The separate checker-self replacement produced the same false PASS; fingerprint `archive-gate|false-green|A-controlled-checker-overrides-validator|77bdf71|writer`, count `1`. Neither Red used a missing module, mock, or artificial assertion.
- [x] 4.5 **Green/Refactor.** Execute all archive-Gate judge authority from the exact candidate `C` Git bytes: a fixed direct-argv bootstrap loads the candidate checker in memory, and that checker loads its candidate runner validator in memory, without executing either A-worktree copy. Treat the A copies only as compared archive subjects. Do not duplicate the validator or add a general Git-fixture/archive framework. Preserve every existing parent/R100/path/canonical/protected/clean/stdout boundary.
  - Green evidence: the same runner-replacement test and the separate checker-self replacement test both PASS, while the exact positive still returns exit `0`, stdout exactly `archive-gate=PASS\n`, and empty stderr. The fully materialized one-line task 6.2 bootstrap was also executed against an exact local `C→A` fixture and returned the same exact PASS contract. The checker contains no archive validator copy: it only retrieves `C:tools/lifecycle-receipts/profile_runner.py` by direct Git argv and calls that trusted function.
- [x] 4.6 **Replacement candidate.** Update the literal-SHA archive recipe to run trusted-C checker bytes, rerun all writer Gates, then safely amend the branch tip so the replacement remains one commit with sole parent `44aea3b3f892acf8a6e0537f65f28f33de22cf2f`. Prove old object `77bdf711575c390e5bc56b2d5eaf3a8f1e6caa64` remains readable, record both SHAs, and leave both independent verifier Gates `NOT_RUN` for the replacement.
  - Writer evidence: focused `25/25`, full receipt discovery `52/52`, archived forward `7/7`, runner contract, exact historical-three matrix, corrective clean-preview chain, strict, whitespace/owned/protected/bare-variable/sensitive/no-shell, offline Go format/test/race/vet/build/smoke, JS, and `JSON OK 44` all PASS. `git cat-file -e 77bdf711575c390e5bc56b2d5eaf3a8f1e6caa64^{commit}` exited `0`. The staged owned-path correction becomes the replacement atomically through `git commit --amend --no-edit` in this isolated change branch; this changes no main ref and performs no reset/rebase. The handoff supplies the new literal SHA, and the same old-object/readable plus new-parent/clean checks rerun after amend. Tasks 5.1-5.3 remain unchecked for the replacement.

## 5. Independent Exact-SHA Verification

- [ ] 5.1 **Minimal-context exact-candidate recipe.** Required task input: repository, change name, and literal full repair-candidate SHA. Workdir: a new verifier-created clean detached worktree at that SHA. Run serially, in order: `git rev-parse HEAD` (exit `0`, stdout exactly the input SHA); `git symbolic-ref -q HEAD` (exit `1`, empty stdout); `git status --porcelain=v1 --untracked-files=all` (exit `0`, empty stdout); `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tools/lifecycle-receipts/tests/test_post_archive_bootstrap_binding.py -v` (runs the archive-checker fixture positive and all A-controlled checker/runner, canonical-byte/judge/path/parent/rename/HEAD negatives; exit `0`, empty stdout, stderr ends in `OK` with no skip); `git diff --no-ext-diff --quiet` (exit `0`, empty stdout); `git rev-parse HEAD` (exit `0`, same literal SHA); and `git status --porcelain=v1 --untracked-files=all` (exit `0`, empty stdout). The handoff MUST include exact `threadId`, `hostId`, optional typed `wait_cursor` as `wait_threads.afterCursor`, SHA, result, and limitations; no prior chat, writer result, parallel command, network, secret, or repository write is an input.
  - Invalidated old evidence: thread `019ffef3-ac44-7c22-9c4a-060af02bb954` passed only rejected SHA `77bdf711575c390e5bc56b2d5eaf3a8f1e6caa64`; any replacement content/SHA requires a fresh session and full rerun.
- [ ] 5.2 **Ordinary exact-candidate recipe.** Required task input and worktree contract are repeated independently: repository, change name, literal full repair-candidate SHA, and another new verifier-created clean detached worktree. Execute serially; parallel commands are forbidden. Preflight: `git rev-parse HEAD` (input SHA/exit `0`); `git symbolic-ref -q HEAD` (empty stdout/exit `1`); `git status --porcelain=v1 --untracked-files=all` (empty stdout/exit `0`). Run these actual commands: `openspec validate allow-post-archive-bootstrap-binding --strict`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tools/lifecycle-receipts/tests/test_post_archive_bootstrap_binding.py -v` (including archive-checker fixture positive and A-controlled checker/runner, canonical-byte/judge/path/parent/rename/HEAD negatives); `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tools/lifecycle-receipts/tests -p 'test_*.py'`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest openspec/changes/archive/2026-08-13-persist-archived-lifecycle-receipt/checks/test_verify_forward.py`; `PYTHONDONTWRITEBYTECODE=1 python3 openspec/changes/archive/2026-08-13-persist-archived-lifecycle-receipt/checks/verify_runner_contract.py --repo .`; `PYTHONDONTWRITEBYTECODE=1 python3 tools/lifecycle-receipts/profile_runner.py --repo . --all-historical --json`; `PYTHONDONTWRITEBYTECODE=1 python3 tools/lifecycle-receipts/verify_receipt.py --repo . --chain --json`; `test -z "$(gofmt -l services/api)"`; `GOPROXY=off GOTOOLCHAIN=go1.26.5 go test ./services/api/...`; `GOPROXY=off GOTOOLCHAIN=go1.26.5 go test -race ./services/api/...`; `GOPROXY=off GOTOOLCHAIN=go1.26.5 go vet ./services/api/...`; `GOPROXY=off GOTOOLCHAIN=go1.26.5 go build ./services/api/...`; `GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/smoke.sh`; `find apps -type f -name '*.js' -print0 | xargs -0 -n 1 node --check`; `node -e 'const fs=require("fs"),path=require("path");const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>{const p=path.join(d,e.name);return e.isDirectory()?walk(p):[p]});const files=[...walk("apps").filter(f=>f.endsWith(".json")),"project.config.json"].filter(fs.existsSync);for(const f of files)JSON.parse(fs.readFileSync(f,"utf8"));console.log("JSON OK "+files.length)'`; `git diff --check 44aea3b3f892acf8a6e0537f65f28f33de22cf2f...HEAD`; and `git diff --name-only 44aea3b3f892acf8a6e0537f65f28f33de22cf2f...HEAD`. Every command exits `0` except detached symbolic-ref; strict stdout is exactly `Change 'allow-post-archive-bootstrap-binding' is valid`; both exact Gate outputs are reviewed as bytes in `C`; unittests end `OK` without skip; historical JSON contains exactly three profiles with expected two `MECHANICAL_PASS`/one `EXPECTED_MECHANICAL_FAIL`; chain JSON preserves old `FAILED` and actor `NOT_PROVEN_BY_MECHANICAL_REPLAY`; whitespace/gofmt/JS/diff-check stdout is empty; JSON parse stdout is `JSON OK 44`; name-only stdout contains only declared owned paths; Go/smoke output contains no FAIL. Postflight: `git diff --no-ext-diff --quiet` (exit `0`), `git rev-parse HEAD` (same literal SHA), and `git status --porcelain=v1 --untracked-files=all` (empty). Record every command/result plus exact `threadId`, `hostId`, optional `wait_cursor` typed as `wait_threads.afterCursor`, SHA, result, and limitations.
  - Rejected evidence: thread `019ffef3-ac44-7c22-9c4a-062a6f2dbcc4`, host `local`, exact `77bdf711575c390e5bc56b2d5eaf3a8f1e6caa64` returned FAIL despite every declared command PASS; fingerprint `archive-gate|false-green|A-controlled-runner-overrides-validator|77bdf71|verifier`, count `1`. It cannot satisfy or be reused for a replacement SHA.
- [ ] 5.3 The main Gate calls `read_thread` with each current verifier's exact `threadId` and `hostId`, then enters `INDEPENDENT_VERIFIED` only when both finals report unchanged exact candidate PASS with `C9/T10/V>=9/R9`, every item at least 8, and hard blockers zero. `wait_cursor` means only `wait_threads.afterCursor`; it is optional, may be caller-local, and is never reused as `read_thread`'s separate pagination cursor. Audit pointers are not proof; an unreadable final, stale/mismatched final, or any implementation/spec/task/command/SHA change returns `NO-GO` and requires new exact-SHA verification.

## 6. Authorized Integration and Archive

- [ ] 6.1 With separate authorization, pure-FF the dispatcher-provided literal verified candidate into clean local main. In that exact workdir run `git rev-parse HEAD` (stdout exact candidate, exit `0`) and `git status --porcelain=v1 --untracked-files=all` (empty, exit `0`), then rerun integrated Gates without creating the binding, receipt, push, PR, deploy, or external write.
- [ ] 6.2 **Exact archive recipe.** Dispatcher task input MUST contain repository, literal full candidate SHA, and literal full archive SHA. Before execution it replaces the final two placeholders below with those two 40-hex literals—no angle brackets, shell SHA variables, command substitution, branch, moving ref, or A-worktree checker path may remain: `PYTHONDONTWRITEBYTECODE=1 python3 -c 'import re,subprocess,sys;c,a,r=sys.argv[1:4];re.fullmatch(r"[0-9a-f]{40}",c) is not None or sys.exit(1);re.fullmatch(r"[0-9a-f]{40}",a) is not None or sys.exit(1);p="openspec/changes/allow-post-archive-bootstrap-binding/checks/verify_archive.py";q=subprocess.run(["git","-C",r,"cat-file","blob",f"{c}:{p}"],check=False,capture_output=True,timeout=30);q.returncode == 0 or sys.exit(1);n=f"{c}:{p}";sys.argv=[n,"--repo",r,"--candidate",c,"--archive",a];exec(compile(q.stdout,n,"exec"),{"__name__":"__main__","__file__":n})' <literal-full-candidate-SHA> <literal-full-archive-SHA> .`. This fixed bootstrap only loads exact-`C` checker bytes; that checker loads exact-`C` runner-validator bytes. Workdir is clean local-main at exact archive SHA, and both A copies are comparison subjects only. Preflight: `git rev-parse HEAD` outputs the literal archive SHA/exit `0`; `git symbolic-ref -q HEAD` outputs the local-main branch/exit `0`; `git status --porcelain=v1 --untracked-files=all` is empty/exit `0`. Run the fully materialized command once; expected exit is `0`, stdout is exactly `archive-gate=PASS` plus one LF, stderr is empty. Any other exit/output, A-controlled checker/runner authority, wrong parent, non-same-relative/non-`R100` move, path, canonical byte, protected blob, HEAD, or clean-state result is `NO-GO`; strict/name-status alone cannot compensate. Postflight: `git diff --no-ext-diff --quiet` exits `0`; `git rev-parse HEAD` repeats the literal archive SHA; status remains empty. Only then may this repair be `ARCHIVED`; Goal0 remains `NO-GO`.
- [ ] 6.3 Hand off literal full candidate/archive SHAs, both current candidate verifier pointers, exact archive command output, frozen runner identity, and still-`NOT_RUN` Goal0 fields. Handoff is this change's last task and MUST NOT claim binding-head, receipt-head, receipt closure, or Goal0 PASS.

## 7. Goal0 External Delivery Dependency — not this change's lifecycle tasks

These Gates are tracked by the main Agent after this repair is `ARCHIVED`; they do not keep this change open, but any missing/failed/out-of-order Gate keeps Goal0 `NO-GO`:

- **G0.1 binding creation recipe — authorized integrator only.** Dispatcher input: literal trusted archive SHA. After appending only the fixed fourth object and committing, the integrator returns literal full binding-head SHA and works at clean local-main `HEAD` equal to it. Run `git rev-parse HEAD` (binding-head SHA/exit `0`); `git status --porcelain=v1 --untracked-files=all` (empty); `git diff-tree --no-commit-id --name-status -r HEAD` (exact stdout `M` plus tab plus `tools/lifecycle-receipts/mechanical-bindings-v1.json`); `git show HEAD^:tools/lifecycle-receipts/mechanical-bindings-v1.json | PYTHONDONTWRITEBYTECODE=1 python3 -c 'import json,sys; d=json.load(sys.stdin); assert len(d["bindings"]) == 3'` (exit `0`, empty stdout); and `git show HEAD:tools/lifecycle-receipts/mechanical-bindings-v1.json | PYTHONDONTWRITEBYTECODE=1 python3 -c 'import json,sys; d=json.load(sys.stdin); assert len(d["bindings"]) == 4 and d["bindings"][-1]["profile_id"] == "lifecycle-receipt-control-v1"'` (exit `0`, empty stdout). No candidate/archive/registry/wrapper/executor/receipt byte may change. Any other path, role, count, or output is `NO-GO`.
- **G0.2 binding-head verifier recipe — different independent session.** Required task input: repository, `persist-archived-lifecycle-receipt`, and literal full binding-head SHA. Workdir: a new verifier-created clean detached worktree at that SHA. Run serially: `git rev-parse HEAD` (input SHA/exit `0`); `git symbolic-ref -q HEAD` (empty/exit `1`); `git status --porcelain=v1 --untracked-files=all` (empty/exit `0`); `PYTHONDONTWRITEBYTECODE=1 python3 tools/lifecycle-receipts/profile_runner.py --repo . --profile lifecycle-receipt-control-v1 --json` (exit `0`; JSON profile `lifecycle-receipt-control-v1`, target `d0b70a077bcaa64c401837eb0e9b6f27035210a0`, `mechanical_verification=MECHANICAL_PASS`, `actor_independence=NOT_PROVEN_BY_MECHANICAL_REPLAY`); `git diff --no-ext-diff --quiet` (exit `0`); `git rev-parse HEAD` (same input SHA); and `git status --porcelain=v1 --untracked-files=all` (empty). The main Gate MUST use `read_thread(threadId, hostId)` to read the current exact SHA/PASS final; optional `wait_cursor` means only `wait_threads.afterCursor`. Mechanical JSON alone is insufficient. No receipt may exist before PASS.
- **G0.3 receipt creation recipe — integrator only after G0.2 live PASS.** Create only `openspec/changes/archive/2026-08-13-persist-archived-lifecycle-receipt/lifecycle-receipt.md` with original exact candidate/integration/archive facts, the fixed binding hash, audit-only attestation, and both derived markers `REQUIRED_DERIVED`; commit and return literal full receipt-head SHA. In clean local-main `HEAD` equal to that SHA run `git rev-parse HEAD` (receipt-head SHA/exit `0`); `git status --porcelain=v1 --untracked-files=all` (empty); and `git diff-tree --no-commit-id --name-status -r HEAD` (exact stdout `A` plus tab plus the receipt path). Any earlier, multi-path, duplicate, or persisted derived PASS is `NO-GO`.
- **G0.4 receipt-head verifier recipe — another independent session, not G0.2.** Required task input: repository, `persist-archived-lifecycle-receipt`, and literal full receipt-head SHA. Workdir: another verifier-created clean detached worktree at that SHA. Run serially: `git rev-parse HEAD` (input SHA/exit `0`); `git symbolic-ref -q HEAD` (empty/exit `1`); `git status --porcelain=v1 --untracked-files=all` (empty/exit `0`); `PYTHONDONTWRITEBYTECODE=1 python3 tools/lifecycle-receipts/verify_receipt.py --repo . --change persist-archived-lifecycle-receipt --json` (exit `0`; JSON exact candidate `d0b70a077bcaa64c401837eb0e9b6f27035210a0`, profile `lifecycle-receipt-control-v1`, `mechanical_verification=MECHANICAL_PASS`, receipt head equal to the literal input, `receipt_head_verification=PASS_DERIVED`, `receipt_head_result_persisted=false`, actor `NOT_PROVEN_BY_MECHANICAL_REPLAY`); `git diff --no-ext-diff --quiet` (exit `0`); `git rev-parse HEAD` (same input SHA); and `git status --porcelain=v1 --untracked-files=all` (empty). The main Gate MUST use `read_thread(threadId, hostId)` to read the current exact SHA/PASS final; optional `wait_cursor` means only `wait_threads.afterCursor`. Only that live final completes Goal0; the derived result MUST NOT be written back.
