## Red

- [x] 1. 先测量问题范围，不靠推断。写一次性扫描器跑遍全部 `.wxml`。
  - 首版没有跟踪引号，在 `wx:if="{{qty > 0}}"` 上立刻报出四个假阳性（`<stepper>`、`<money>` 被当成未闭合）。属性值里的 `>` 是这类解析器最容易踩的坑。
  - 改为逐字符跟踪引号后，全仓**只有 `pages/profile/profile.wxml` 一处**，与开发者工具的报错一致。
- [x] 2. 追溯来源：`8ea91e6` 删「我的优惠券」行、`8b6118c` 删「设置」行之后，`<view class="card group">` 空了，闭合标签留在原地。
- [x] 3. 写 `checks/check_wxml_balance.py`，十项。
  - 三项「坏模板必须被拒」、五项「好模板不得误报」、一项 `wx:elif` 非回归、一项仓库现状。
  - 断言方式是喂模板看 lint 的返回码与报文，不断言 lint 源码里出现了什么词。
- [x] 4. base_sha `25edc70` 上运行 → `WXML_BALANCE_GATE=FAIL (4/10)`。
  - 首版只有 3 红：「仓库全部平衡」一项**平凡通过**，因为它信了被测 lint 的结论，而 base 的 lint 根本不查配对。补上门禁自带的独立解析后变 4 红，真实缺陷才被 Red 覆盖。门禁不该拿被测对象的结论当证据。

## Green

- [x] 5. 删除 `profile.wxml` 第 46 行多余的 `</view>`，「切换身份入口」与底部占位回到 `.pad body` 内。
- [x] 6. 扩展 `tests/lint_wx.py`：层级栈每层由「上一个兄弟的条件类型」改为 `[cond, name, line]`，弹出时比对标签名。
  - 复用既有 `TAG` 正则 —— 它的 `"[^"]*"` 分支已经正确处理属性值内的 `>`，重写只会把第 1 步那个坑再踩一遍。
  - void 清单提取为常量 `VOID`，标签配对与 `wx:elif` 检查共用，不各自维护。
  - 三类错误分别报并给出成对另一端的行号：工具只能指到错误暴露处（第 50 行），要改的却是另一端（第 46 行）。

## 本地验证

- [x] 7. `WXML_BALANCE_GATE=PASS`（10/10）。
- [x] 8. `python3 tests/lint_wx.py .` → `WX_LINT=PASS`。
- [x] 9. `node --test tests/*.test.js` → 102 pass / 0 fail，无回归。
- [x] 10. 归档门禁 33 项与 base 逐行一致，**无接管**。
- [x] 11. UI1：门禁本身即 UI1 证据 —— 它把八份构造模板喂给真实的 lint 进程并检查返回码与报文，而不是读源码断言。`profile.wxml` 的平衡由门禁自带解析独立复核。

## 本 change 之外的发现

- `profile.wxml` 第 31–35 行仍有「外卖配送 · 暂未开放」一行，与 §5.9「**不含**：我的优惠券、会员中心、外卖配送」冲突，也属 §0.2 禁止的占位提示。这是范围问题不是结构问题，另开 change 处理，本 change 不顺手改。

## 独立验证

- [ ] 12. 提交产生候选 SHA。
- [ ] 13. 在干净 detached worktree 对该精确 SHA 只读重跑 7–11。
