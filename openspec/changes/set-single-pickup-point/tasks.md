## Red

- [x] 1. 写 `checks/check_pickup_point.js`，九项。
- [x] 2. base_sha `53a98db` 上运行 → `PICKUP_POINT_GATE=FAIL (8/9)`。
  - 其中一项把一处既有不一致直接打了出来：`checkout wrote 县前大厦 1F · 2 号取餐窗口, the seed carries 党政办公中心后院老食堂北门` —— `confirm.pay()` 写的是取餐窗口描述，种子写的是点位名称，同一字段两条写入路径装着不同语义的值。
- [x] 3. 记录基线：小程序 102 pass / 0 fail；归档门禁 35 项，14 项 FAIL。

## 门禁自身的两次收窄（均在实现前修正）

- [x] 4. 「清扫废弃演示文案」原按 `['县前直营店', '县前大厦']` 全仓扫。但 `县前直营店` 同时是 `STORE.branch`（门店名），客户这次只给了取餐地点，一刀切会逼我把一个明显虚构的值换成一个看起来可信的虚构值 —— 后者更难在 UAT 时被发现。收窄为只扫取餐点语境，并补一条「`pickupPoint` 不得写字面量」。
- [x] 5. 「取餐地点只展示一次」原按页面数据的 JSON 计数，把 `store.pickupWindow` 也算了进去，而模板并不渲染它。改为断言模板绑定数。

## Green

- [x] 6. 两端新增 `PICKUP_POINT = '党政办公中心后院老食堂北门'` 并导出；`PICKUP_POINTS` 收敛为 `[{ name: PICKUP_POINT }]`。
- [x] 7. 两端全部订单快照与 `SETTINGS.pickupPoint` 改为引用常量而非字面量，杜绝漂移。
- [x] 8. `confirm.pay()` 改写常量，与种子一致。
- [x] 9. 订单详情去掉 `ptAddr` 与 `PICKUP_POINTS.find(...)`，只展示一次取餐地点。
- [x] 10. **PC 三点位表收敛为单点**。`data/seed.js` 原有三条记录（县前直营店 / 绥芬河北站取餐点 / 青云镇综合市场点），营业设置页据此渲染 `<select>` —— 正是 §3.1「不提供多点选择或分单路由」排除的能力。改为只读输入框。
- [x] 11. **删除 `STORE.pickupWindow`**（两端）。它与常量表达同一件事；一个事实存两处正是第 2 步那处分裂的成因。结算页改用常量。
- [x] 12. PRD §13.3：该行标为已确认并写入正式值；另起一行记录门店名与门店地址仍为演示值、待客户提供。

## 本地验证

- [x] 13. `PICKUP_POINT_GATE=PASS`（9/9）。
- [x] 14. `node --test tests/*.test.js` → 102 pass / 0 fail，无回归。
- [x] 15. `python3 tests/lint_wx.py .` → `WX_LINT=PASS`。
- [x] 16. 归档门禁 35 项与 base 逐行一致，**无接管**。
- [x] 17. UI1：门禁驱动真实 `Page` 完成「加购 → 结算 → 打开订单详情」，断言写入值与种子一致、详情只渲染一次取餐地点。

## 本 change 之外

- 门店名 `绥安食品 · 县前直营店` 与门店地址 `绥芬河市青云镇通商路` 仍为演示值。客户这次只给了取餐地点，凭空替换会把一个明显虚构的值换成看起来可信的虚构值。已在 PRD §13.3 单列一行标记待提供。

## 独立验证

- [ ] 18. 提交产生候选 SHA。
- [ ] 19. 在干净 detached worktree 对该精确 SHA 只读重跑 13–17。
