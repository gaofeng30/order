## 1. Approval and frozen boundary

> 状态：`CANDIDATE`。`gate_type=W3`、`ui_level_target=UI0`、`ui_level_actual=UI0`、`base_sha=2b83e93cc2a8d2bb16b606068028f34ee662b677`、`candidate_sha=external-post-commit`。Writer Gate 已在最终实现字节上通过；exact SHA 由提交后 Git 与 handoff 绑定。真实微信仍为 `BLOCKED_EXTERNAL/NOT_RUN`，independent verification 尚未运行。

- [x] 1.1 Re-read proposal/design/spec/tasks/checkpoint, run `openspec validate establish-miniprogram-user-session --strict`, and obtain explicit main-session approval before recording `DRAFT → APPROVED`; confirm `gate_type=W3`, `ui_level_target=UI0`, exact base, owner/worktree, owned paths, dependencies, non-goals, local MySQL Gate and real-WeChat `BLOCKED_EXTERNAL/NOT_RUN` are consistent.
  - Evidence: 2026-08-20 main session explicitly reviewed all five planning artifacts and returned `APPROVED`; lifecycle then recorded `DRAFT → APPROVED → IMPLEMENTING`. `openspec validate establish-miniprogram-user-session --strict` is rerun after this transition; this proves executable planning only, not implementation behavior.
- [x] 1.2 Before the first implementation edit, confirm HEAD is still based on `2b83e93cc2a8d2bb16b606068028f34ee662b677`, no other writer owns migration/router/main, and no P1–P5, phone, employee, merchant/RBAC, protected endpoint, order/payment, frontend or production-secret scope has entered the change.
  - Evidence: branch is `codex/establish-miniprogram-user-session`; `HEAD` and merge-base both equal exact base `2b83e93cc2a8d2bb16b606068028f34ee662b677`; main session retained the same Writer/branch/worktree and serialized migration/router/main. Scope audit finds excluded subjects only in non-goal language. Real WeChat remains `BLOCKED_EXTERNAL/NOT_RUN`.

## 2. Red

- [x] 2.1 Add provider tests that assert the fixed GET path/query, three-second timeout, redirect refusal, single attempt, 16 KiB body cap, strict documented JSON, `40029`/`40226` rejection, transient/unknown/protocol failures, and AppSecret/code/openid/`session_key` canary secrecy; run `GOPROXY=off GOTOOLCHAIN=go1.26.5 go test ./services/api/internal/wechat -run '^TestCode2Session' -count=1` before implementation and record the first target-behavior failure.
  - Red evidence: exact focused command exit `1`; first decisive error is `code2session_test.go:48:12: undefined: newCode2SessionClient`, followed by the absent credentials/runtime constructor/typed errors. `candidate_sha=not-yet-created`; controlled local test source only; no network or real WeChat call occurred; this proves missing provider behavior, not Green or platform acceptance.
- [x] 2.2 Add identity crypto/service tests for 32-byte `crypto/rand`, unpadded URL-safe token, SHA-256-only persistence, one-time return, 24-hour injected-clock expiry, exact expiry rejection, multiple active sessions, random-source failure and collision failure; run `GOPROXY=off GOTOOLCHAIN=go1.26.5 go test ./services/api/internal/identity -run '^Test(Session|Token)' -count=1` and record the first target-behavior failure.
  - Red evidence: exact focused command exit `1`; first decisive error is `service_test.go:25:13: undefined: newService`, followed by absent `ErrUnauthenticated` and `CreateSessionParams`. No service/repository implementation existed; `candidate_sha=not-yet-created`; this proves missing session behavior only.
- [x] 2.3 Add strict handler/router tests for the exact POST/201 contract, 1 KiB exact JSON, 400/401/503 envelopes, no unversioned route, wrong-method 405, no provider/DB calls for invalid requests, and captured-log secrecy; run `GOPROXY=off GOTOOLCHAIN=go1.26.5 go test ./services/api/internal/identity ./services/api/internal/httpapi -run '^(TestMiniProgramSession|TestRouter)' -count=1` and record the first target-behavior failure.
  - Red evidence: exact focused command exit `1`; first decisive error is `handler_test.go:122:42: undefined: SessionIssuer`, followed by absent `NewHandler`; router compile also reports the old four-argument `NewRouter`. No HTTP implementation existed; `candidate_sha=not-yet-created`.
- [x] 2.4 Add v8/v9 schema assertions and real MySQL 8 integration tests for migration v1→v9/repeat, exact columns/keys/check/FK, first login, same-openid concurrent login yielding one user/two sessions, collision/statement/commit rollback, last-login rollback, hash lookup and expiry boundary; run `GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/miniprogram-session-integration.sh` before schema/repository implementation and record the first W3 failure. A mock or SQLite result is not valid Red/Green evidence.
  - Schema Red evidence: writer-owned MySQL `8.0.46` container `order-session-w3` was healthy on loopback with tmpfs storage; the exact command exited `1`, first decisive failure `migration count = 7, want 9`. After v8/v9, the identical command passed twice against real MySQL, proving v1→v9/repeat and exact schema.
  - Repository Red evidence: after adding only persistence behavior tests, the same exact W3 command exited `1`; first decisive error was `undefined: NewRepository`. Minimal transaction repository then made the unchanged command exit `0`; after removing redundant test helpers, Refactor rerun also exited `0`. The race-enabled real-MySQL suite proves one user/two sessions under same-openid concurrency, collision rollback including `last_login_at`, CHECK statement rollback, forced commit failure rollback, hash lookup and exact expiry.
- [x] 2.5 Extend config/main/smoke tests with AppID/AppSecret canaries and catalog/menu anonymous regressions, then run the focused checks before wiring to prove missing configuration/session routing while the existing anonymous contract still passes.
  - Config Red evidence: `GOPROXY=off GOTOOLCHAIN=go1.26.5 go test ./services/api/internal/config -run '^TestLoadMiniProgram' -count=1` exited `1`; first decisive error was `cfg.MiniProgram undefined`. The unchanged command passed after minimal structured credential loading and again in Refactor.
  - Main/smoke Red evidence: after adding only canary and route/anonymous regression assertions, `GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/smoke.sh` exited `1`; first decisive compile error was `not enough arguments in call to httpapi.NewRouter`, proving missing identity wiring. Minimal main wiring made the unchanged command output `smoke: PASS`; its Refactor rerun also passed. The smoke uses no real WeChat call and proves invalid JSON returns before provider/DB, only the versioned route exists, catalog/menu remain anonymous, and credential canaries are absent from logs.

## 3. Green

- [x] 3.1 Implement `internal/wechat` with fixed official HTTPS endpoint, typed non-sensitive errors, exact one-attempt request/response handling and package-local test endpoint injection; rerun 2.1 unchanged to Green.
  - Green evidence: unchanged focused command exit `0` (`ok .../internal/wechat`); controlled tests cover exact query, runtime three-second timeout, redirect refusal, one attempt, strict documented response, body cap and typed failures. Refactor evidence: after gofmt and retaining the minimal two-constructor boundary, the identical command exits `0` again. No real WeChat asset/network call was used or claimed.
- [x] 3.2 Add migrations `000008_create_miniprogram_users.sql` and `000009_create_miniprogram_sessions.sql`, update every affected migration name/count/latest-version assertion, and add `internal/identity` repository transaction with unique-openid upsert, hash-only insert and internal active-session lookup.
  - Green/Refactor evidence: the unchanged W3 command passed after the additive one-statement v8/v9 migrations and again after minimal repository implementation/refactor. All affected embedded/catalog/menu/migrate latest-version assertions now require the exact v1–v9 chain.
- [x] 3.3 Implement identity token/service behavior with runtime `crypto/rand.Reader`, injected test clock/reader, microsecond UTC timestamps, fixed 24-hour TTL, commit-before-return and fail-closed errors; rerun 2.2 and the real MySQL command from 2.4 unchanged to Green.
  - Green/Refactor evidence: the exact 2.2 focused command exits `0` after the minimal service and again after gofmt; the unchanged race-enabled real MySQL command from 2.4 exits `0` after atomic persistence and again after Refactor, proving commit-before-return, concurrent user uniqueness, multiple sessions, rollback and expiry.
- [x] 3.4 Implement the strict identity handler and single versioned route, extend `httpapi.NewRouter`, and wire config/main without global auth middleware or catalog/menu changes; rerun 2.3 unchanged to Green.
  - HTTP Green/Refactor: exact 2.3 command exits `0` for identity/httpapi after the strict handler and five-dependency router, then exits `0` again after gofmt. Duplicate/unknown/trailing/oversize/blank JSON returns stable 400 and issuer calls remain zero. The exact smoke command passes twice after main constructs the fixed WeChat client, repository, service and handler.
- [x] 3.5 Extend development/test structured config, preserve production secret fail-closed behavior, update the smoke script and dedicated W3 integration script, and rerun 2.5 unchanged to Green with all sensitive canaries absent.
  - Green/Refactor evidence: focused config command passes twice, the full config package passes, smoke passes twice, and the dedicated race-enabled W3 script passes twice. Only structured development/test AppID/AppSecret values exist; production still rejects environment AppSecret before retaining `production_secret_source_unavailable`.

## 4. Refactor and writer Gate

- [x] 4.1 Remove duplication without changing the contract, then rerun the exact provider, identity, handler/router and real-MySQL Red/Green commands; record one evidence block per decisive command using the quality-gate template.
  - Evidence: all four unchanged focused commands exited `0` on final production behavior. After the sensitive-evidence audit removed value-bearing test failure messages, provider, service and handler/router commands were rerun and remained PASS; final MySQL W3 rerun also passed under `-race`.
- [x] 4.2 Run migration and regression checks: `GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/mysql-integration.sh`, `GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/catalog-integration.sh`, and `GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/menu-integration.sh`; verify catalog/menu success/error JSON and anonymous access remain exact.
  - Evidence: first MySQL regression run exposed only a missing local canary credential in its real API subprocess; adding those structured test env values made the same command PASS. On final bytes, mysql, catalog and menu scripts all exited `0`; catalog/menu handlers remained anonymous and exact across success/error integration paths. No session request or real WeChat request occurred in that subprocess.
- [x] 4.3 Run `test -z "$(gofmt -l services/api)"`, `GOPROXY=off GOTOOLCHAIN=go1.26.5 go test ./services/api/...`, `GOPROXY=off GOTOOLCHAIN=go1.26.5 go test -race ./services/api/...`, `GOPROXY=off GOTOOLCHAIN=go1.26.5 go vet ./services/api/...`, `GOPROXY=off GOTOOLCHAIN=go1.26.5 go build ./services/api/...`, and `GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/smoke.sh`; retain only sanitized decisive outcomes.
  - Evidence: final-byte sequential writer run exited `0` for gofmt, all Go tests, all race tests, vet, build and smoke; smoke printed only `smoke: PASS`. The route smoke sends only invalid JSON that terminates before service/provider/repository and contains no replayable login value.
- [x] 4.4 Run `openspec validate establish-miniprogram-user-session --strict`, `git diff --check 2b83e93cc2a8d2bb16b606068028f34ee662b677...HEAD`, and an owned-path audit that includes untracked files; prove no Authorization, secret, code, openid, `session_key`, raw token/hash or provider body appears in logs/evidence/diff fixtures.
  - Evidence: strict and working-tree diff checks exited `0`; the explicit status-path allowlist including untracked files printed `owned-path audit: PASS`. Runtime logging contains only stable reasons/path/status; provider and persistence errors discard transport/SQL details; response/log secrecy tests and smoke canary scans pass. Test fixtures are explicit synthetic canaries only, and failure messages were tightened so Gate output cannot print their values. No real credential, login value, provider identity/body, Authorization value or raw session/hash appears in evidence.
- [x] 4.5 Record actual Red/Green/Refactor/writer evidence, `ui_level_actual=UI0`, C/T/V/R with total `>=36` and every score `>=8`, zero hard blockers, and real WeChat still `BLOCKED_EXTERNAL/NOT_RUN`; then commit only owned paths, record the full `candidate_sha`, and confirm worktree/index clean. No candidate exists before this task succeeds.
  - Writer verdict prepared: `{ gate_type: W3, ui_level_target: UI0, ui_level_actual: UI0, base_sha: 2b83e93cc2a8d2bb16b606068028f34ee662b677, candidate_sha: external-post-commit, score: C9/T10/V8/R9=36, hard_blockers: 0, local_mysql: PASS, real_wechat: BLOCKED_EXTERNAL/NOT_RUN, independent_exact_sha: NOT_RUN }`. V is capped at 8; exact SHA and clean state are derived after the immutable owned-path commit and handed to a different verifier.

## 5. Real WeChat delivery external Gate

- [ ] 5.1 Keep real official `code2Session` acceptance `BLOCKED_EXTERNAL/NOT_RUN` until the customer Mini Program administrator and developer provide a real AppID/AppSecret, authenticated account, reachable network and a fresh real `wx.login` code; local stubs MUST NOT satisfy this task.
- [ ] 5.2 Under separate authority and non-sensitive evidence handling, run one real exchange and verify the final server session result without recording the code, openid, `session_key`, AppSecret, raw token/hash or provider body; otherwise leave delivery NO-GO while allowing unrelated local work.

## 6. Independent exact-SHA verification

- [ ] 6.1 After task 4.5 only, a different verifier checks out the full `candidate_sha` in another clean detached worktree, confirms exact HEAD/clean status/owned paths, and reruns strict, focused provider/API/service tests, real MySQL W3/concurrency/rollback/expiry, catalog/menu regressions, all Go test/race/vet/build and smoke checks without modifying candidate bytes.
- [ ] 6.2 The verifier records PASS or the first sanitized failure for that exact SHA. Any code, migration, proposal, design, spec, task, base, acceptance command, rebase, merge or SHA change invalidates the result and returns the same Writer lane to Red/Green/Refactor.

## 7. Authorized integration boundary

- [ ] 7.1 Only after an unexpired exact-SHA independent PASS and separate integration authorization, integrate into current local main; if main advanced, create a new candidate and rerun the complete writer and independent Gates.
- [ ] 7.2 Verify the integrated main contains only the accepted owned result and record `INTEGRATED`. Push, PR, deploy, production/external writes, real-platform calls and archive remain separately authorized actions and are not part of this task.
