## 1. Boundary and approval

- [x] 1.1 Confirm proposal/design/spec/tasks/checkpoint consistently declare one `miniprogram-primary-phone` outcome, `gate_type=W3`, `ui_level_target=UI0`, `ui_level_actual=NOT_RUN`, exact base, owner/worktree, owned paths, read-only contracts, dependency, non-goals, external assets and the single ACCEPT/REJECT verdict; run `openspec validate bind-miniprogram-primary-phone --strict` and obtain explicit main-session approval before recording `DRAFT → APPROVED`.
  - Evidence: main session completed full DRAFT review and explicitly returned `APPROVED`; the same Writer recorded `DRAFT → APPROVED → IMPLEMENTING`. `openspec validate bind-miniprogram-primary-phone --strict` exited `0` with `Change 'bind-miniprogram-primary-phone' is valid`; branch/base checks matched `codex/bind-miniprogram-primary-phone` and `73cf1e04d6661ad08cbd2b03c2bb8324d4769f4d`. This proves implementation admission only; all behavior, MySQL and real-platform Gates remain unrun.

## 2. Red

- [x] 2.1 Add package-local wechat tests for fixed stable-token/getPhoneNumber method/path/query/body, current openid, watermark, strict JSON/body cap/timeout/redirect, one-attempt transport, concurrent refresh merge, `expires_in` early refresh, invalid-token no replay and sensitive canaries; run `GOPROXY=off GOTOOLCHAIN=go1.26.5 go test ./services/api/internal/wechat -run 'Test(StableToken|GetPhoneNumber|PhoneClient)' -count=1` before production code and record the first decisive failure.
  - Red evidence: exact focused command exited `1`; first decisive compile failure was `phone_number_test.go:62:12: undefined: newPhoneNumberClient`, followed by absent runtime constructor/fixed endpoints/typed rejection. No provider production file existed; `candidate_sha=not-yet-created`, external asset `N/A`, and this does not prove Green, MySQL or real WeChat.
- [x] 2.2 Add phone service/handler/router tests for exact Bearer/JSON/success/error envelopes, already-bound provider bypass, E.164/masking, no global middleware and same-code rejection recovery; run `GOPROXY=off GOTOOLCHAIN=go1.26.5 go test ./services/api/internal/identity ./services/api/internal/httpapi -run 'Test(Phone|BindPhone|RouterPhone)' -count=1` before implementation and record the first decisive failure.
  - Red evidence: exact focused command exited `1`; first decisive failure was `phone_handler_test.go:20:37: undefined: PhoneBinding`, followed by absent phone errors/interfaces/user model/handler and `NewRouter` still accepting only five dependencies. No identity/router phone production behavior existed; `candidate_sha=not-yet-created`, external asset `N/A`, and global middleware was not edited.
- [x] 2.3 Add v10 embedded/schema assertions and a real MySQL 8 integration suite for v1→v10/repeat, exact columns/unique/CHECK, existing NULL rows, first bind, same-phone idempotency, different-phone same-user concurrency, cross-user unique conflict, statement/commit rollback, bound-at preservation and same-code recovery; run `GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/miniprogram-phone-integration.sh` before migration/repository implementation and record the first W3 failure. Mock/SQLite is not valid W3 evidence.
  - Initial schema Red evidence: a writer-owned `order-phone-w3` container using MySQL `8.0.46-oraclelinux9` was healthy on an isolated dynamic localhost port; the test process required the `order-mysql-w3`/`YES` ownership markers and did not skip. The exact dedicated command exited `1`; its first failure was `TestMiniprogramSessionMySQLIntegration: migration count = 9, want 10`, followed by the phone migration test reporting the same missing v10. Repository concurrency and rollback Red remains required before this item is complete.
  - Repository Red evidence: after migration Green, the complete behavior suite was added while production repository methods were still absent. The unchanged dedicated W3 command exited `1`; first failure was `repository.FindPhoneUser undefined`, followed by missing `BindPrimaryPhone` and the concrete repository not satisfying `PhoneStore`. This was a real compile Red inside the non-skipping MySQL command, before repository implementation.
- [x] 2.4 Extend main/smoke and catalog/menu/session regression assertions before wiring, then run `GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/smoke.sh` to prove the protected route is absent while anonymous/existing routes remain exact.
  - Red evidence: smoke was first extended for the unique versioned route's exact missing-Bearer 401, unversioned/profile 404, unchanged anonymous endpoints and a no-log phone-code canary; owned catalog/menu/session migration regressions already required v10. Before main wiring, the exact smoke command exited `1`; first failure was `main.go: not enough arguments in call to httpapi.NewRouter`, specifically the absent `*identity.PhoneHandler`.

## 3. Green

- [x] 3.1 Add the fixed official in-process stable-token manager and getPhoneNumber client under `internal/wechat`, including package-local endpoint injection only, exact-one-attempt transport, cache merge/early refresh, openid/watermark validation and typed sanitized errors; rerun 2.1 unchanged to Green.
  - Green/Refactor evidence: after only `phone_number.go` plus gofmt, the exact 2.1 focused command completed with exit `0`, then the unchanged command completed again after structural review. Controlled tests prove one stable-token wire for 12 concurrent misses, five-minute early refresh, short-TTL non-reuse, invalid-token eviction without replaying the current code, and three provider POSTs over three distinct HTTP/1.1 connections against an HTTP/2-capable TLS server. Fixed endpoints, request bodies, openid, watermark, E.164, redirect/timeout/body cap/strict JSON and canary-safe typed failures pass; real WeChat remains `BLOCKED_EXTERNAL/NOT_RUN`.
- [x] 3.2 Add single-statement v10 and update every owned embedded/catalog/menu/migrate/identity migration assertion from v9 to v10 without editing v8/v9; rerun the real MySQL command from 2.3 to the first next failure.
  - Green evidence: added only `000010_add_miniprogram_primary_phone.sql` as one LF-terminated `ALTER TABLE`; v8/v9 bytes were untouched. The unchanged dedicated W3 command then exited `0` with `ok .../internal/identity 2.109s`, proving v1→v10, v9→v10, repeat, exact nullable columns/unique/pair CHECK, preserved pre-v10 NULL rows and rejected partial pairs on writer-owned MySQL 8. Repository behavior tests were not yet present, so this is migration Green only.
- [x] 3.3 Add only `identity/phone_*` repository/service/handler behavior: pre-provider bound read, transaction-free provider call, locked binding transaction, E.164/masking, stable errors and same-code recovery; rerun 2.2 and 2.3 unchanged to Green.
  - Green/Refactor evidence: the exact 2.2 command passed after the real repository was added (`identity 0.452s`, `httpapi 0.852s`). The dedicated W3 command passed first in `1.889s` and, after reviewing the lock/unique/rollback boundary without changing the contract, passed unchanged again in `1.655s`. Real MySQL proves first bind, same-phone/time preservation, one-winner different-phone concurrency, one-owner cross-user concurrency, statement/commit rollback and rejected-code recovery; the provider remains outside the repository transaction.
- [x] 3.4 Extend router/main with the route-specific phone handler and existing session Authenticate, then update smoke and the dedicated W3 script without global middleware or profile/compatibility routes; rerun 2.4 unchanged to Green.
  - Green/Refactor evidence: main reuses one identity repository and the existing session service for both session creation and phone-route Authenticate, constructs the fixed-endpoint phone client, and passes only the phone handler to router. The exact smoke command returned `smoke: PASS` twice. Its missing-Bearer phone request returns 401 before Authenticate/binder/provider, while anonymous health/catalog/menu/session, unversioned 404 and absent profile remain exact; no global middleware was added.

## 4. Refactor and writer Gate

- [x] 4.1 Remove duplication without changing the contract, then rerun the exact provider, handler/router and real-MySQL Red/Green commands; record one sanitized quality-gate evidence block per decisive command.
  - Refactor evidence: exact provider focused PASS (`wechat 0.478s`), exact service/handler/router focused PASS (`identity 0.444s`, `httpapi 0.845s`), and exact dedicated W3 PASS (`identity 1.848s`). A fixture-only timeout hang was diagnosed once as `httptest.Server.Close` waiting on handlers that blocked on server request contexts; a subtest-local release channel now lets client timeout/one-call assertions finish before server close, without production changes. Real WeChat remains `BLOCKED_EXTERNAL/NOT_RUN`.
  - Final boundary RGR: review found the one-digit lower E.164 bound was incorrectly rejected by masking. `TestPhoneServiceMasksMinimumE164` failed under the unchanged 2.2 command; changing only the byte-length lower bound made that command pass in `0.881s/0.447s`, then pass unchanged in `0.232s/0.383s`. No public contract changed.
- [x] 4.2 Run migration/regression scripts: `GOPROXY=off GOTOOLCHAIN=go1.26.5 bash services/api/scripts/mysql-integration.sh`, `... catalog-integration.sh`, `... menu-integration.sh`, the existing `... miniprogram-session-integration.sh`, and the new `... miniprogram-phone-integration.sh`; verify session creation and catalog/menu anonymous contracts remain exact.
  - W3 regression evidence: on the same isolated healthy MySQL 8 runtime, all five exact scripts exited `0`: migrate `4.161s`; catalog/httpapi `1.899s/2.038s`; menu/catalog/httpapi `2.053s/1.824s/1.634s`; session `1.468s`; phone `1.572s`. These are non-skipped real-MySQL runs and preserve anonymous catalog/menu and existing session creation contracts.
  - Final-byte replay after the masking fix also exited `0` for all five scripts: migrate `3.650s`; catalog/httpapi `1.993s/1.587s`; menu/catalog/httpapi `1.423s/2.011s/1.557s`; session `1.631s`; phone `1.611s`.
- [x] 4.3 Run `test -z "$(gofmt -l services/api)"`, `GOPROXY=off GOTOOLCHAIN=go1.26.5 go test ./services/api/...`, `... go test -race ./services/api/...`, `... go vet ./services/api/...`, `... go build ./services/api/...`, and `... bash services/api/scripts/smoke.sh`; retain only sanitized decisive outcomes.
  - Final-byte writer evidence: gofmt check, full Go test, full race, vet and build all exited `0`; smoke returned `smoke: PASS`. Full test/race included every API package; frontend/JS is `N/A` because this W3/UI0 change owns no frontend paths.
- [x] 4.4 Run `openspec validate bind-miniprogram-primary-phone --strict`, `git diff --check 73cf1e04d6661ad08cbd2b03c2bb8324d4769f4d...HEAD`, and an owned-path audit including untracked files; prove full phone/code/access-token/AppSecret/openid/Authorization/session/provider-body canaries do not appear in logs, errors or evidence.
  - Prepared-candidate evidence: strict OpenSpec, working-tree diff check, explicit untracked-inclusive owned-path audit, v8/v9 byte guard, one-statement migration check, script syntax, unique production phone-route audit, main provider-origin exclusion and new-production-file log/dynamic-error scan all exited `0`. Provider/handler/smoke canary tests prove sensitive values do not reach errors, responses or logs. The immutable `base...candidate` diff check is rerun externally immediately after commit because a commit cannot contain its own object ID.
- [x] 4.5 Record actual Red/Green/Refactor/writer evidence, `ui_level_actual=UI0`, `C/T/V/R >= 36` with every dimension at least 8 and zero hard blockers; then commit only owned paths, record full `candidate_sha`, and confirm worktree/index clean. Real WeChat remains `BLOCKED_EXTERNAL/NOT_RUN`; no candidate exists before this task succeeds.
  - Candidate preparation: `ui_level_actual=UI0`; `C9/T10/V8/R9=36`; every dimension is at least 8 and hard blockers are `0`. Only declared owned paths are prepared for the single candidate commit. The exact full SHA and clean worktree/index are bound as external post-commit Harness/handoff evidence; failure of commit, exact diff or cleanliness invalidates this item. Real WeChat remains `BLOCKED_EXTERNAL/NOT_RUN` and is not counted as PASS.

## 5. Real WeChat delivery external Gate

- [ ] 5.1 Keep real getPhoneNumber acceptance `BLOCKED_EXTERNAL/NOT_RUN` until the customer Mini Program administrator and developer provide an authenticated non-personal subject, enabled capability with usable quota, real credentials, reachable official network and one fresh user-click code; local stubs MUST NOT satisfy this task.
- [ ] 5.2 Under separate authority and non-sensitive evidence handling, run one authenticated real binding and verify the final masked API result without recording any credential, identity, code, token, phone or provider body; otherwise leave delivery NO-GO while allowing unrelated local work.

## 6. Independent exact-SHA verification

- [ ] 6.1 After task 4.5 only, a different verifier checks out full `candidate_sha` in a clean detached worktree, confirms exact HEAD/clean state/owned paths, and reruns strict, focused provider/API/service, real MySQL W3, all migration/catalog/menu/session regressions, all Go test/race/vet/build and smoke checks without changing candidate bytes.
- [ ] 6.2 Verifier records exact-SHA PASS or the first sanitized failure. Any migration/code/proposal/design/spec/task/base/acceptance-command/rebase/merge/SHA change invalidates the result and returns this same Writer lane to Red/Green/Refactor.

## 7. Authorized integration boundary

- [ ] 7.1 Only after an unexpired exact-SHA independent PASS and separate integration authorization, integrate into current local main; if main advanced, create a new candidate and rerun complete writer and independent Gates.
- [ ] 7.2 Verify integrated main contains only accepted owned results and record `INTEGRATED`. Push, PR, deploy, production/external writes, real-platform calls and archive remain separately authorized and are not part of this task.
