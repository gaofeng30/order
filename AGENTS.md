# 项目协作规则

## 第一性原理

- 必须使用第一性原理思考，从真实业务目标、客户主体责任、平台规则、成本风险和可验证的交付结果出发。
- 不能默认用户已经完全知道自己想要什么、为什么要这样做，以及怎样得到正确结果。
- 只有歧义会改变业务行为、公共契约、数据结果、责任或授权边界、验收方式时才暂停，并只问一个带推荐答案的关键问题；否则说明最小假设后继续。
- 不隐藏不确定性；存在多种会改变上述结果的合理解释时，必须明确分歧点并等待确认或逐项 grill。
- 用户已确认的产品决策和现有公共契约属于硬约束，不得自行改变。

## 方案规范

- 采用满足需求的最短正确路径，只处理需求直接相关的范围。
- 方案必须明确、单一、可执行；不得提供模糊多选、过渡双轨或假设未来需求的方案。
- 不新增需求或现有契约未要求的兼容分支、降级、兜底、抽象、配置或未来接口。
- 不允许过度设计，也不得顺手优化、重构或处理相邻问题。
- 修改前明确具体范围和最小成功标准；涉及业务规则、公共契约、权限责任或上线流程时，验证必须覆盖请求范围内的完整链路。

## OpenSpec 变更边界

- 根 `AGENTS.md` 是跨工具硬规则的单一事实源，`.agents/skills/` 是项目执行流程的单一事实源；Claude、Codex 和 Cursor 入口只做薄引用，不复制规则。
- 任何会改变产品行为、公共契约、数据、架构、研发流程或部署方式的工作，都必须对应一个 `openspec/changes/<change-name>/`。
- 实现前必须完成并读取 `proposal.md`、`design.md`、`specs/*/spec.md` 和 `tasks.md`，并通过 `openspec validate <change-name> --strict`。
- 一个 change 只承载一个可独立理解、实现、验收和回滚的主能力；可以分别验收或回滚的结果必须拆开，不得用大 spec 汇总多个需求。
- change 必须声明 owner、owned paths、依赖、非目标和可执行验收命令；会改变业务结果的未决项不得进入实现。
- 状态流统一为：`DRAFT → APPROVED → IMPLEMENTING → CANDIDATE → INDEPENDENT_VERIFIED → INTEGRATED → ARCHIVED`。

## Worktree 与并行

- 每个 change 使用独立 branch 和 worktree；同一 change、同一路径同时只有一个 writer。
- 无依赖、无公共契约冲突且 owned paths 不重叠的 changes 可以并行，不得被其他 change 的本地 Gate 全局阻塞。
- 存在依赖或共享路径时，必须在 OpenSpec 中声明依赖或重新划分所有权后再开工。
- 不得直接在 `main` 上提交。提交只包含当前 change 的 owned paths，不得覆盖或回退其他人改动。

## Harness 开发闭环

- 开工先运行 `./tools/harness status`，从当前 checkout、OpenSpec、Git 和本地控制账本恢复 change、task、阻塞与下一步；状态为 `UNKNOWN` 时不得猜测。
- 主控制 Agent 只在获得当前证据后运行 `./tools/harness checkpoint`；本地账本只是可重建的调度索引，不替代 OpenSpec、exact-SHA、独立验证、集成、归档或运行证据。
- 每次交接或收尾运行 `./tools/harness check`；失败必须先处理首个 `WHAT / WHY / FIX`，不得降级 Gate 或把未验证写成 PASS。
- 可复现的流程摩擦使用 `./tools/harness observe` 留证；当前模块只记录不自改，只有后续独立 OpenSpec change 通过完整 Gate 并集成后，规则才从下一模块生效。

## TDD 与实现

- 行为实现严格遵循 Red → Green → Refactor：先得到可观察的失败证据，再完成最小实现，重构后重跑同一验证。
- 文档、配置、迁移和文件移动使用链接、结构、契约、迁移或内容完整性检查建立 Red/Green 证据，不编造无价值测试。
- 每完成一项就在 `tasks.md` 勾选，并在条目下记录决定性命令、结果或设计理由。

## 独立验证与集成

- 实现 worktree 自测通过后产生候选 SHA；验证者必须在另一个干净的 detached worktree 对该精确 SHA 只读验证。
- 验证者不得修改业务文件。验证失败后由 writer 在实现 worktree 修复并产生新 SHA。
- 实现、规格、任务或 SHA 在验证后发生任何变化，旧验证立即失效，必须重新验证。
- 只有依赖满足且精确 SHA 独立验证通过的 change 才能集成；rebase 或 merge 新 `main` 后视为新候选并重验。
- change 只有集成 `main` 后才能 archive。未获单独授权不得推送、创建 PR、部署或变更外部系统。

## Grill 规则

- 客户资料、平台权限、支付规则、业务规则和运维责任需要逐项 grill。
- 只有已经被用户明确确认通过的事项，才能写入客户正式文档。
- 官方文档明确要求的事项可以写入，但必须标明用途、是否必填和对应后台位置。
- 未确认的建议、推断或行业经验只能进入待确认队列，不能伪装成客户已确认结论。

## 市场参考规则

- 遇到产品推荐、交互建议、业务规则、风控策略或“最佳实践”判断时，可以参考市面已有头部点单、餐饮、外卖、自提类产品。
- 需要参考市场做法时，使用 agent-reach 调研或搜索公开资料，并优先参考官方文档、头部产品公开说明、可验证的真实产品流程。
- 市场做法只能作为建议来源，不能直接替代客户确认；写入客户文档前仍需逐项 grill 通过。

## 责任边界

- 客户持有小程序账号、管理员身份、主体资质、微信支付商户号、收款账户和经营责任。
- 开发方负责开发、配置、联调、提审、上线协助、技术排障和运维支持。
- 为了交付效率，开发方可以在项目成员可授权范围内尽量开放权限，但不得混同为客户管理员、收款主体或经营责任人。
