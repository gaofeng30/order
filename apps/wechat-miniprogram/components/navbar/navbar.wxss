:host { display: block; }
.navbar { width: 100%; z-index: 50; }
.navbar.block { background: var(--bg); flex: 0 0 auto; }
.navbar.block.dark { background: transparent; }
.navbar.overlay { position: absolute; top: 0; left: 0; right: 0; background: transparent; }

.nb-row { position: relative; display: flex; align-items: center; padding: 0 16rpx; }
.nb-back {
  width: 60rpx; height: 60rpx; display: flex; align-items: center; justify-content: center; margin-left: 4rpx;
}
.nb-left { font-size: 32rpx; font-weight: 600; margin-left: 8rpx; }
.nb-left.light { color: var(--ink); }
.nb-left.dark { color: #fff; }
.nb-title {
  position: absolute; left: 50%; transform: translateX(-50%);
  font-size: 32rpx; font-weight: 600; font-family: var(--font-head); letter-spacing: 1rpx;
  max-width: 50%; text-align: center; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
}
.nb-title.light { color: var(--ink); }
.nb-title.dark { color: #fff; }
.nb-right {
  position: absolute; top: 0; bottom: 0; display: flex; align-items: center; gap: 24rpx;
}
